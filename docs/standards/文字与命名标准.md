# 文字与命名标准

> 定位：文件命名与目录结构、跨文件引用格式、用词规则与禁用清单、术语用法与治理词条、Markdown 写法与 AI 友好写作——「叫什么名、用什么词、怎么写文」三件事都以本件为准。
> 依据：四件合并而成；§6 禁用清单界标段由守卫 `scripts/check_content.py` 逐行读取。

## 目录

- 一、条文
- 二、正例与反例
- 三、脚本检查点

## 一、条文

### 1. 目录结构与命名总表

```text
Aether/
├── AGENTS.md                  # 入口与路由：红线、路由、常用命令、模型执行通用准则
├── README.md                  # 门面：安装、使用、文档索引
├── CHANGELOG.md               # 变更日志（Keep a Changelog 格式）
├── docs/
│   ├── standards/             # 标准件：skill资产／文字与命名／产物契约／运行环境
│   ├── product/               # 产品层：共享件在根，域件在子文件夹（中文命名）
│   └── specs/                 # 施工机制.md／施工问题记录.md＋在制规格与审查产出；验收后移入 archive/
│       └── archive/
├── skills/                    # skill 资产（运行时：域壳与域内资产）
└── scripts/                   # 开发守卫与工具（仓库级）
```

| 文件类型 | 命名规则 | 示例 | 位置 |
| --- | --- | --- | --- |
| 域名 | `{skill 名}`：英文 kebab-case（Agent Skills 规范硬约束），与 skill 目录同名；域名清单见[架构设计](../product/架构设计.md) 域清单 | — | `skills/` |
| skill 主文件 | `SKILL.md`（固定名，每 skill（域）一份） | — | `skills/{skill 名}/` |
| 域工作流 | `{域}-workflow.md` | `example-workflow.md` | `skills/{skill 名}/references/` |
| 阶段文件 | `{域}-stage-{阶段名}.md` | `example-stage-research.md` | 同上 |
| 域内参考文件 | `{域}-{主题}.md`，主题描述性命名 | `example-scaffold-spec.md` | 同上 |
| 产物模板 | `{域}-{产物名}-template.md`；默认独立成文件，内嵌仅限 ≤10 行微型模板 | `example-artifact-template.md` | **`skills/{skill 名}/assets/`** |
| 运行时脚本 | `{动词}-{对象}.{sh,py,js}` | `init-project.js` | `skills/{skill 名}/scripts/`（预留） |
| 共享库 | `{名词}-utils`（被同 skill 脚本引用，不直接执行） | `format-utils.js` | 同上（预留） |
| 一键入口 | `check.sh`（固定名） | — | 仓库根 `scripts/` |
| 开发守卫 | `check-{对象}.{sh,py,js}` | `check_content.py` | `scripts/`（仓库级） |
| 开发测试 | `test-{对象}.{sh,py,js}` | 无实例，预留类目（预立法，随首批实例校准；**关联实例判定标准：`ls scripts/test-*` 命中 ≥1**） | `scripts/`（仓库级） |
| 仓级开发工具 | `{动词}-{对象}.{sh,py,js}`（`.py` 按表注用 snake_case） | `patch_file.py` | `scripts/`（仓库级） |
| 规格 | `{序号NNN}-{YYYY-MM-DD}-{中文主题}.md`；序号三位递增、永不复用 | `059-2026-09-12-填充标准.md` | `docs/specs/`；验收通过后移入 `archive/` |
| 标准 | `{主题}标准.md`，中文命名，主题短而自描述 | `skill资产标准.md`、`文字与命名标准.md` | `docs/standards/` |
| 协作机制 | `施工机制.md`（固定名） | — | `docs/specs/` |
| 施工问题记录 | `施工问题记录.md`（固定名，踩坑台账） | — | `docs/specs/` |
| 评审意见 | `{对象}评审意见.md`（一次性文档——整改后归档） | `开发侧评审意见.md` | `docs/specs/` |
| 产品文档 | `{主题}.md` 中文命名 | `产品定义.md` | `docs/product/`（共享件在根；域件在 `outline/`／`volume/`／`write/` 子文件夹） |
| 根目录治理文件 | 固定名（生态惯例） | `AGENTS.md`、`README.md`、`CHANGELOG.md`、`LICENSE` | 仓库根 |
| 根目录工程配置 | 固定名（工具惯例） | `.gitignore`、`.gitattributes`、`.markdownlint-cli2.jsonc` | 仓库根 |
| 插件清单（预留） | `.claude-plugin/marketplace.json` | — | 仓库根（预留） |
| 命令薄代理（预留） | `{skill 名}-{路由键}.md`，单行内容写调用行；启用前不得新增实例 | — | `.claude/commands/`（预留） |
| agent 文件（预留） | `{skill 名}-{角色名}.md`；T1 触发后启用——启用前不得新增实例 | — | `.claude/agents/`（预留） |
| 用户项目产物 | 中文命名，不进本仓库——细则由产品层契约件定义 | — | 用户工作目录 |

表注（扩展名）：`.py` 用 snake_case，`.sh`／`.js` 用 kebab-case——上表含 `.py` 后缀的四行以本表注为准。
表注（域前缀）：`{域}-` 前缀适用于需消歧的 skill 资产；治理文档不加前缀——位置由文件夹表达，名字表达内容。
表注（规格行）：同批附件沿用主规格序号、以语义后缀区分；作废批移入 archive 时文件名追加 `-已作废`。

### 2. 双语命名纪律

- 英文 kebab-case：skill 目录、references、脚本、守卫——开发者可见的一切；规格主题部分用中文（作者阅读的治理记录）。
- 中文名：产品文档与用户项目内的产物文件。
- 路径分隔符一律正斜杠（反斜杠在 Unix 上失效）。

### 3. 文件名安全

文件名禁用 Windows 保留字符 `\/:*?"<>|`，禁 emoji，不用空格；仓库内文件一律英文 kebab-case 或本表规定的中文名，同样禁符号。

### 4. 跨文件引用格式

| 场景 | 格式 | 示例 |
| --- | --- | --- |
| 同目录文件（仓库治理文档） | Markdown 链接 | `[产物契约标准.md](产物契约标准.md)` |
| 跨目录文件（仓库治理文档） | Markdown 链接（含相对路径） | `[AGENTS.md](../../AGENTS.md)` |
| skill 资产内引用（无论同目录跨目录） | 禁链接与裸文件名，用 skill 根相对路径文字 + 节名（**指向仓库治理文档时按本表末行**） | `references/outline-workflow.md`「草稿步」节 |
| 文件名提及（无需跳转） | 行内代码 | `CHANGELOG.md` |
| 指定小节（治理文档） | 文件名 + 节名 | `AGENTS.md`「红线」节 |
| 指向仓库治理文档（**部署副本内不存在**） | **维护出处：`{件名}` §N；运行时不必加载** | **维护出处：`书目录契约` §1；运行时不必加载**（本项目例） |

规则：skill 运行时资产（`references/`、`assets/`、`scripts/`）禁跨目录链接——部署副本会断链，必须自包含；仓库治理文档允许跨目录链接。references 之间不互相链接（一层深纪律，见[skill资产标准](skill资产标准.md) §13）。

**运行时资产引用治理文档**：skill 资产内不得无标注地指向仓库治理文档。凡**模型执行时必须知道**的事实（命令名、字段名、阈值）一律**自包含**写在资产内；仅供维护追溯的出处写「**维护出处：{件名} §N**」并明示「运行时不必加载」。（阶段指向型包外标记——如「随对应阶段批」——不属本条，按到期制处理；措辞统一用「随对应阶段批」。）

### 5. 用词规则

1. **一词一义**：同一概念全仓只用一个词；发现第二种叫法当次改齐（改一处，或把旧叫法登进 §6）。
2. **禁比喻借词**：不得借用实物、建筑、机械类比喻表达机制；机制叫什么，就写它做什么。
3. **禁中文单字缩写**：不得把单字当名词用（如「态」「面」单独成词）；须写全词。
4. **禁自造代号**：不得用自造简称指代检查项、流程或文件；编号是标签，引用处须复述它查什么。
5. **禁英文残留**：正文不得混用英文词（产品与生态固有名词、文件与命令名除外）；确无中文对应者，先入 §6 再统一替换。
6. **一句一事**：一句只写一件事；并列条件拆成短句。

### 6. 禁用清单

> 以本段为准。**守卫 `scripts/check_content.py` 逐行读本段**；格式＝一行一条「旧词 ＋ 箭头 ＋ 新词」。迁移期守卫**新旧双读**（见 §7）；新增旧词须**先在正文发现实例**再写入本段（不预先立法）。

<!-- 用词禁用清单：以本段为准；守卫 scripts/check_content.py 逐行读本段 -->
```text
闸 → 检查关
门禁 → 提交前自动检查
锚点 → 位置
机检锚点 → 脚本检查点
现状锚点 → 现状位置
基线 → 起点
骨架 → 结构
承重 → 关键
咬合 → 衔接
热路径 → 每批必读
冷路径 → 按需读
快路径 → 小改流程
逃生口 → 解冻条件
兜底 → 保底做法
射程 → 适用范围
壳 → skill 主文件
落点 → 写入位置
落位 → 位置
归宿 → 归到哪条规则
断链 → 找不到对应
空转 → 空跑
自噬 → 自我匹配
标尺 → 参照标准
底盘 → 基础设定
处方 → 修改建议
梯队 → 分层名单
挂载 → 纳入
装载 → 读入
冻结 → 停止改动
自由度 → 可自定程度
台账 → 清单
核销 → 逐条确认
对账 → 核对
销号 → 标记已解决
挂账 → 记在清单上
清账 → 清单清零
禁改面 → 禁改文件
改动面 → 改动文件
回写面 → 需同步更新的文件
检查面 → 检查脚本
守卫面 → 守卫脚本
单维 → 单项
双维 → 两项
态闸 → 状态检查
判据 → 判定标准
机检 → 脚本检查
走查 → 人工试跑
人判 → 人工判断
对规 → 对照模板核对
真源 → 以…为准
唯一真源 → 以…为准
唯一出处 → 以…为准
反模式 → 施工问题记录
漂移 → 不符
声索 → 声称
顶替 → 代替
定格 → 最终形态
档位 → 流程
交底 → 开工说明
收口 → 收尾
止损 → 停手
埋没 → 遗漏
触碰 → 涉及
连带 → 相关
口径 → 计算方式
沉淀 → 积累成文
落盘 → 写入文件
回写 → 同步更新
定级 → 判定流程
勘验 → 现状调查
立例 → 定为规则
判三 → 同步更新文件核对
判四 → 流程判定核对
夹具 → 测试样例
探针 → 占位标记
置红 → 判为失败
三态 → 三种结果
假阳性 → 误报
白名单 → 允许清单
黑名单 → 禁止清单
findings → 发现的问题
门控 → 阶段确认点
停靠 → 中途暂停
廉价检查点 → 不阻断的通报点
```
<!-- 用词禁用清单完 -->

### 7. 迁移做法

- **生效面**：
  - 文件类：`AGENTS.md`／`docs/standards/**`／`docs/specs/施工机制.md`／`docs/product/**`／`skills/**`／`README.md`。
  - 脚本类：`scripts/**` 的注释、docstring 与输出文案。
  - 对话类：与作者的对话回复（经 `AGENTS.md` §八）。
  - 条文对**新写入文本**即时生效；存量已清零（守卫输出措辞已同步，见 §7 迁移做法）。
- **脚本例外（三类，均在 `scripts/**`）**：① 双读判据里的旧词（如 `现状锚点|现状位置`——兼容归档规格）② 一形两义的词（「壳层」指 shell，不是 skill 主文件）③ 引文（守卫注释里引用的归档规格原文）。三类是功能形态或引文，不迁。
- **不适用面**：`docs/specs/archive/**`、在制批次规格、`CHANGELOG.md`——过程记录与历史证据，改写即篡改；读旧件时按 §6 清单翻译理解，不改原处。
- **迁移分三批**：三批已于 2026-09-17 全部完成（批 1＋2＝开发侧基础设施重构；批 3＝产品层与 `skills/`；同日另清 `scripts/` 注释与输出文案）。存量已清零；§6 此后只对**新写入文本**生效。
- **覆盖边界**：本清单只网已列出的词；单字缩写家族由 §5 第 3 条与人工审查兜。**已知子串死角**：禁用词含常用子串（「对规」⊂「对规格」、「档位」⊂「归档位置」、「壳」⊂「壳层」）——遇此类正常措辞改写绕开或标注例外，不删清单词。

### 8. 可用项目词

**通用项目词（机架自带）**：规格／批／施工／审查／核验／候选／断言／守卫／红线／不做清单／缺口表。

产品领域词：量级／爽点／心流／人物卡／标尺（参考标尺）／底盘（世界观底盘）／处方（处方报告）／定格（状态值）。（**说明**：其中四个与 §6 清单同形——那是**同名两义**：清单管治理比喻、本段管产品产物名；守卫按本段豁免。本段为项目插件，复用机架时重写。）

判定标准：该词只在一处定义、全仓只有一个意思、各处用法一致——三条都满足即保留；缺任一条，登进 §6。

两表分工（120 立）：本段「产品领域词」是**用词扫描的豁免面**（`check_content.py` §7 实时读本段）；[`术语表`](../product/术语表.md) 是**产品侧正名记录**（叫什么、不叫什么）。两表不同源不冲突：一个管「扫描时放过哪些词」，一个管「产品文档里用哪个词」。

### 9. 术语用法与治理词条

- 首次出现的术语必须有定义，或指向[术语表](../product/术语表.md) 与本节词条表；发现第二种叫法当次收尾。词条与叫法以[术语表](../product/术语表.md) 为准（产品语言）；本节管用法与治理／资产类词条。
- 术语优先取既有通用词——预训练里已有的说法能免费招募先验；生造词须在同一处给出定义。每条 ＝ 中文名 + 一句话定义 + 英文对应；**例外**：平台标准词可径作词条名（如 `subagent`——宿主生态既定叫法），此时英文对应列与词条名同形。

| 术语 | 定义 | 英文对应 |
| --- | --- | --- |
| subagent | 宿主中由主会话派生的 AI 执行体（**独立上下文**＝全新会话、不复用施工期记忆）；由规划方在会话内 spawn、无物化文件、不进用户书项目；两种用途：工序角色（现行默认）与实体 agent（运行时物化，待 T1 触发） | subagent |
| skill 资产 | 随安装分发到用户机器的 skill 运行时文件（references／assets／scripts），引用规则受部署自包含约束 | skill asset |
| 仓库治理文档 | 仓库根目录与 `docs/` 下的规范、记录类文件——不随 skill 安装分发，引用可用 Markdown 链接 | governance doc |
| 调用双层 | skill 触发方式的分层裁定：skill 入口唯一——`/{skill 名}` 显式调用（自然语言不触发）；skill 间禁互调；预留命令薄代理属 user-invoked 编排层——双层指「直呼 skill／薄代理编排」两级入口 | model-invoked / user-invoked |
| 作者 | 纯裁决位——规格确认、提交确认两处常规出手，另有治理微调的会话内逐项确认与重大分歧裁决 | author |
| 候选类发现（简称候选） | 机器检出但非阻断的发现——只呈报、不影响退出码 | candidate finding |
| 红起点 | 当前已知失败集合；建设期内「未建设」不计入（判定标准见[施工机制](../specs/施工机制.md) §六） | red baseline |
| 负向流程守卫 | 「不许发生某事」的检查（与「拦截式宿主 hook」相对） | negative-path guard |
| 维护出处 | skill 资产内指向仓库治理文档的标注形态——标明该出处仅供维护追溯、部署副本内不存在、运行时不必加载 | maintenance pointer |

### 10. 结构与排版规则

1. 标题用 H1–H4：全文唯一 H1 居首，小节从 `##` 起，逐级递进禁止跳级；H4 仅用于同一 H3 内部的并列分条（如「执行要点」下与表内名字同名的展开），不得整节只含一个 H4；更深层次用列表表达。
2. 代码块标语言（`bash`／`json`／`yaml`／`markdown`，目录树用 `text`）；中文话术、模板示例的围栏不标语言。
3. 超 100 行文件头部放节目录——手动列表（`- 节名`），一级条目与 H2 标题逐字一致，H3 子条目缩进挂靠；目录可置于头部引注块内。
4. 单文件超过 300 行应考虑拆分（skill references 上限见[skill资产标准](skill资产标准.md) §9）。
5. 无序列表统一 `-`；子列表缩进以 markdownlint 实判为准。表格上下留空行；单元格内竖线用 `\|` 转义。段落之间空一行；文件结尾保留一个空行。
6. 不使用 HTML 注释与 HTML 标签（界标段除外——守卫定位用，见 §6）。条文分组用 `章.节[.条]` 前缀，脚本检查点表以该前缀引用。
7. 中英文之间加空格；中文与数字之间加空格；行内代码与全角标点邻接不加空格。全角标点与其他字符之间不加空格；标点不叠用；中文省略号用 `……`。
8. 引号三分：① 节名与需逐字复现的内容用「」；② 中文普通引语用直引号 `"`（本仓常态）；③ 代码与参数内的引号随语言惯例。
9. **序号形态分工**（同一形态只表达一种层级；由浅到深不颠倒——GB/T 9704 层级序数排法）：

   | 用途 | 形态 | 例 |
   | --- | --- | --- |
   | 章节号（H2） | 中文数字＋顿号 | `## 一、角色与职责` |
   | 小节号（H3，含步骤单元 `N.M`） | 阿拉伯数字＋点 | `### 1.2 信息核验` |
   | 槽位标签（结构化单元） | 圆括号数字 | `（1）作用：（2）具体步骤：` |
   | 条号（条文体分条、步骤内小步） | 阿拉伯数字＋点 | `1. 一词一义：…` |
   | 清单项（并列项） | `-` | `- 英文 kebab-case：…` |
   | 行内枚举（句中并列，不换行） | 圆圈数字 | `①单问可跳②本步暂停` |

   **跨处引用一律写名字不写编号**——编号随插入错位（例：写「见『首问』」不写「走第 3 步」）。禁用的列表序号形态：字母序（`a.`／`a)`）、罗马数字。`[N]` 形态保留给 `check.sh` 段号引用（如 `[3]`），不作列表序号。

### 11. 符号允许清单

清单与状态列的**符号型**取值须出自本允许清单：`✅` 已就绪／已修入｜`🚧` 已立项未建设｜`🔴` 未修入或待处置｜`🔨` 校准中｜`⏳` 待讨论｜`➕` 开放集补充｜`⚠️` 风险或依据强度提示。

- 本允许清单只管**符号型**状态取值；文字型取值不受辖。产品文档可在其头部另设图例界定名单内符号的亚义——不得引入名单外符号，禁纯装饰性 emoji。
- 不属清单与状态列取值的符号不受辖：示例对照与命令输出样例中的正反例标记（`✅`／`✗`／`❌` 等）。

### 12. 不采用的 lint 规则

| 规则 | 理由 |
| --- | --- |
| MD013 行长度上限 | 中文与代码混排不适配 80 列，以段落可读性为准 |
| MD040 代码块语言（全量要求） | 真代码的语言标注由 §10 第 2 条约束；中文话术、目录树、模板围栏无语言可标 |

配置摘要（与 `.markdownlint-cli2.jsonc` 对齐）：上表两条已在配置关闭；MD034（裸 URL）有意保留；MD024 设 `siblings_only`。其余以 markdownlint 默认集为准；新增不采用须改本节并说明理由。

### 13. AI 友好写作

1. 术语一致：全文只用[术语表](../product/术语表.md)与本标准 §9 的叫法，发现第二种叫法当次收尾。
2. 示例优先于规则：教格式、风格或行为要求的，给输入／输出对或 ✅／✗ 对照例——正例给合格形态，反例给典型错误与错在哪；模型靠对照例做模式匹配，胜过靠抽象解释。（规范条文立法须附正反例的义务见《施工机制》§五 条文立法。）
3. 指令用命令式语态（「读文件后再改」）。
4. 引用带节位置：`outline-workflow.md`「草稿步」节。
5. 禁模糊指代：不写「格式同上、参照上文」——分段读取时读者看不到"上下文"；带明确节位置的具体引用不受此限。
6. 禁时间敏感表述：不写「X 年 X 月后用 Y」；只写当前方法，版本沿革一律记 `CHANGELOG.md`。
7. 路径用正斜杠并用反引号包裹。
8. 加粗：单文件 `**` 数 ≤ 行数 ÷ 3（数值上限为保底做法）；定性规则优先——加粗仅用于红线、路径、数字、检查关四类，一段至多一处。计数计算方式＝`**` 标记出现次数；保底做法阈值只针对**叙事型文档**，以表格与编号条目为主要结构者（各标准件、产品层域设计、清单型文档）不计。
9. 箭头 `→` 仅用于流程映射与条件分支，禁作装饰。
10. **底座不写产品层的计数**：治理文档只写指针、指向定数字的那件，不复述产品层会变的数字；确需引用时改写为「见 `X` §N」。
11. **引用须写到能唯一写入位置的那一级**：写 `X §N` 时须是该内容实际所在的那一级（`§一` 与 `§一.3` 混用会读到空节）；判定标准＝照该指针去读，必须直接读到内容。
12. **短句优先**（§5 第 6 条的展开）：一句只说一件事；句子写到两行以上就拆成两句；并列条件用列表，不用长句串联。
13. **一段一主题**：段首句点明本段说什么；段内不引入新主题——新主题另起段或加小节。
14. **结论先行**：对话与报告先说结论／结果，再给依据与细节；条文先立规则，出处与沿革放括注或句尾。
15. **嵌套至多一层**：括注（含破折号补语）只包一层；出现括号套括号、破折号接破折号时，把内层细节拆成子列表或独立句。
16. **直陈句式**：主动、肯定、直接；不用双重否定与「不是不可以」式绕弯；拿不准的事实明写「待定」，不靠含糊措辞遮。
17. **删字不删条件**：压缩措辞时，「须／不得／触发条件／例外／数字」一条不许丢——压缩前先列要点，压完逐条对回核对；只留骨架、丢了要点的简写等同失职。
18. **话术指代显名化**：skill 中说给作者听的逐字话术与选项说明——说话方写「AI」、受话方写「你」；**禁裸「我」与「咱们」**。✅「拿不准的跳过，AI 来推」；✗「拿不准的跳过，我来推」（「我」读不出是人类用户还是 AI）；✗「咱们先把规格聊清楚」→ ✅「接下来跟 AI 一起把规格聊清楚」。治理条文与产物正文不受此限（主语由语境唯一确定）。

> 行文规则对**新写入的文本**即时生效；存量不搞全仓重写，按「改文件当批顺手修」纪律（§18）自然收敛。不设机械检查——中文断句的机器判定不可靠，归人工判断轨。

### 14. 给 agent 读的文档

1. 指针：留在上下文里、指向别处材料并携带触发条件的那句话——`description`、`AGENTS.md` 里的指路行、`SKILL.md` 里的引用行都是它。决定 agent 何时去读的是**指针的措辞**；必需的目标配了弱措辞＝方差 bug，处置顺序是先磨措辞、磨不动才内联。指针做两件事：说清材料是什么、列出该触发的分支（反例：只写「详见标准件」而不写何时该去读）。
2. 两种负荷：新增任何文档或指针都花掉两种预算之一——context load（常载材料每轮烧 token 与注意力）与 cognitive load（人记住「有哪些文档、何时用哪份」的负担——花在需要人工判断处，其余处删掉）。
3. 下沉判定标准是**分支**：每个分支都要用的内联在文件内，只有部分分支会读的才沉下去；行数阈值只是保底做法信号。同一概念的规则、定义与注意放同一节；同一意思写两处（duplication）与碎在多处（scattering）是两种病，一处定义律须同时覆盖这两维。
4. 修剪：环境里能查到的就是答案（配置、目录、`--help` 输出，复述＝缓存）；逐句找 no-op——模型默认就会照做的指令白花负荷；没有任何修剪纪律时，默认命运是陈旧层层沉积。

### 15. 禁令须配正向目标

1. 否定式反弹：以禁令驱动行为会把被禁行为拉进上下文并提高其可用度。条文首选正向写法，直接写要做的事（正例：「改文件前先读全文」；反例：只写「禁止删文件」）。
2. 禁令只在无法正向表达的硬护栏上保留，且须在同一处给出正向目标句。红线（`AGENTS.md` §一）、`施工机制` §五 自洽回扫 的事故族列、`AGENTS.md` §四 不做清单速查形态，均视为已履行本节义务。
3. 存量条文按本节校正：「禁止 X」式条文须补正向目标句；补不出的说明是硬护栏，须同处给出理由与红旗自检。

### 16. 产品层文档范式

适用于 `docs/product/` 杄件与 `README.md` 的域条目。

1. 定义性约束：一句话写出该域区别于显然默认做法的那一件事，写成陈述句（反例：「关键点：本域很重要」式标签不携带信息）。
2. 跑通信号：作者不打开 skill 内部即可自查的信号；不得拿内部合规检查（lint 全绿、断言通过）冒充。
3. 角色定位：一句话标出该文档属链步、一次性配置、周期性，还是随时可用。
4. 常见问题：须来自真实来源，数量忠于证据；宁可少写也不注水。

### 17. 内容主张规范（指令措辞力度）

1. **情态三级**（约束力度从高到低，指令用词必须落在其中一级）：

   | 级别 | 用词 | 语义 |
   | --- | --- | --- |
   | 硬约束 | 必须／禁止 | 违反即错，无裁量空间 |
   | 强约定 | 默认／优先 | 可覆盖，但须说明理由 |
   | 启发式 | 建议／可以 | 模型自主裁量 |

   禁用未分级力度词（「硬性要求」统一写「必须」，「不许」统一写「禁止」）；单条指令的力度词必须唯一、不得叠加（「必须优先」均为无效表述）。

2. **数字断言标来源**：内容中的数字与阈值标注〔书证〕〔实测〕〔自定〕之一；自定阈值必须可被用户指令或 CLI 参数覆盖；无来源标注的数字不得写入新内容。
3. **占位符语法**：系统变量全大写英文（`{SKILL_DIR}`）；业务变量统一中文（`{书名}`、`{平台}`）；枚举选项 `{A/B}`；日期 `{YYYYMMDD}`。同一概念全文一种写法。

### 18. 维护

- 新增文件类型：先过 `AGENTS.md`「不做清单」与内生必要性自问，裁定通过后回写 §1 总表。修改命名规则：先全仓 grep 涉及面。退役文件／概念：从表中删除行，退役记录进 `CHANGELOG.md`。
- 新术语：先查本标准 §9；没有 → 定义后同步更新该节，再在其他文档使用。改叫法：全仓 grep 涉及面（grep 词须含概念近义词）。退役术语：从本标准删除行。
- 违反本规范视同文档 bug：改文件当批顺手修。lint 体检：`npx markdownlint-cli2 "**/*.md"`。
- **不写批号**：长期存在的文件（治理件、产品层、`skills/` 资产、脚本注释）**不写批号**（如「资产已建（088）」）。状态陈述只写状态（「阶段一资产已建」）；沿革与事故出处记 `CHANGELOG` 与 git 历史；确需指认归档规格时用**全路径**（`archive/099-…md`）。裁定标记同理不写（定为规则的依据除外）。
- 对齐类改动先审标准后审文件：对齐批第一步核对标准条文自身的实文依据，再审文件符合度。

## 二、正例与反例

- ✅ `skills/example-skill/references/example-skill-workflow.md`——英文 kebab-case、描述性主题名。
- ✅ `docs/standards/文字与命名标准.md`——治理文档中文命名、无域前缀（位置由文件夹表达）。
- ✅ `check_content.py`——`.py` 用 snake_case（表注计算方式）。
- ✅ §6 禁用清单带两行界标，守卫逐行读该段；清单只列实测有实例的词。
- ✅ 引号分工：「你倾向哪个选题？」（逐字复现话术）｜"降维打击"（普通引语）。
- ✗ `docs/standards/命名规范.md`——标准件后缀不统一（同一概念两种叫法，发现第二种当次收尾）。
- ✗ `tools/check_content.py`——守卫落在 `scripts/` 之外，目录即分类。
- ✗ 路径写 `docs\standards\文字与命名标准.md`（反斜杠在 Unix 上失效）。
- ✗ 模糊指代：「字段格式同上」——须写「字段定义见[书目录契约](../product/书目录契约.md) §2」。
- ✗ 借用建筑或机械部件的比喻表达机制；把单字当名词用；用自造简称指代检查项。
- ✗ 清单段缺界标或缺新词一半：守卫读不到整段即报「用词检查未生效」。

## 三、脚本检查点

| 条文 | 强制方式 | 状态 |
| --- | --- | --- |
| 文件名禁 Windows 保留字符、禁 emoji、无空格 | 模式扫描（可机械化） | 待建 |
| 标准件命名 `{主题}标准.md` 且与目录清单一致 | 目录清单断言 | 待建 |
| 规格命名 `{NNN}-{YYYY-MM-DD}-{中文主题}.md` 且序号不重复 | 清单断言 | 待建 |
| 规格验收后移入 `docs/specs/archive/`（移动不留副本） | `docs/specs/` 根只允许在制规格 | 待建 |
| 路径一律正斜杠 | 文本扫描（可机械化） | 待建 |
| 守卫／测试文件落在 `scripts/` | 清单断言 | 待建 |
| skill 资产内引用不出现相对 `.md` 链接 | 扫描 `references/`／`assets/`／`scripts/` 内相对链接 | 待建 |
| §4「维护出处」标注（资产内指向治理文档须带标注） | 内容轨检查器（接入 `check.sh` [3]） | 已实现 |
| §6 禁用清单 | 内容轨检查器（用词；§8 产品领域词豁免；接入 check.sh [3]） | 已实现（候选，恒不置红） |
| §10 结构规则（标题层级、围栏、表格、尾换行等） | `scripts/check.sh` [1] markdownlint | 已实现 |
| §10.3 超 100 行文件头部节目录且条目与 H2 逐字一致 | 内容轨检查器（TOC 存在性；规格与归档件不适用） | 已实现 |
| §5 第 3 条 单字缩写家族 | 无脚本检查（人工审查；属人工判断） | 无守卫（人工判断） |
| §7 迁移分批 | 无脚本检查（迁移批次由各批规格点名） | 无守卫（人工判断） |
| §13.8 加粗密度 | **不设守卫**（阈值已立：§13.8 行数 ÷ 3 保底） | 无守卫（阈值已立，守卫未设） |
