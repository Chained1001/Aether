# 大纲域设计

> 状态（2026-09-19 同步）：**运行细则与现行结构以 `skills/aether-outline/` 为准**（`references/outline-stage1.md`／`outline-stage2.md`＋`assets/` 两模板＋`scripts/` 两核验器）。本件与分步三件成文于上一设计迭代，与运行侧的已知差异：步骤单元七字段→四槽位（判定标准见[skill资产标准](../../standards/skill资产标准.md) §12.9）；步骤划分与编号以运行侧为准（1.0–1.3／2.0–2.4）；基调 2-4 词→2-6 词；「范围组／视角组」（旧称「可数组／不可数组」）且组号重排（范围①-⑤／视角⑥-⑫）；裸「标尺」即参考标尺。本件存设计沿革与理由，读时以运行侧为准。
> 定位：大纲域（`aether-outline`）的产品设计——域职责、定义性约束、流程总览、写法纪律、评审体系、成本降级与上线验收清单。
> **链步件**：作者按 `/aether-outline` 显式调用进入，沿规格、资料、草稿、打磨定稿四步走到定稿门；分步细则分落[规格与资料步](大纲域设计-规格与资料步.md)／[草稿步](大纲域设计-草稿步.md)／[打磨定稿步](大纲域设计-打磨定稿步.md)／[附录](大纲域设计-附录.md) 四件。
> 依据：迁移自存档《大纲阶段设计文档》v0.9.6（787 行，停止改动版），按现行基座计算方式改写迁入（三域化、去人工试跑编号；旧追踪清单与人工试跑编号索引不迁入）。本域**阶段一（规格步）资产已建**，其余阶段细则随对应建设批落地——运行资产（skill 主文件、`references/`、`assets/`、脚本）随各批落地。

## 目录

- 一、域职责与状态
- 二、定义性约束
- 三、流程总览
- 四、写法纪律
- 五、域交互细则
- 六、评审体系总表
- 七、灵感库接口（可选外部资产）
- 八、成本与降级
- 九、跑通信号（It's working if）
- 十、上线验收清单
- 十一、开放项

## 一、域职责与状态

大纲域（`aether-outline`；**阶段一（规格步）资产已建**，阶段细则构建中）：立项、设定与全书总纲的规划——把「想写一本什么书」先压成一页规格，再沿资料收集、草稿、打磨定稿三步把规格长成全书大纲；产出书目录结构与大纲域产物，文件树、字段与状态值见[书目录契约](../书目录契约.md)。

边界：本域管到**定稿门**为止——卷纲归卷纲域、细纲与正文归正文域；已退役域不参与（创作输入＝作者积累＋资料＋方法论＋灵感库）。

状态：设计定稿件（本组五件）已成文；`skills/aether-outline/` **阶段一（规格步）资产已建**；`references/`／`assets/` 与脚本随各批落地。

## 二、定义性约束

**约束集先行**：本域的一切创作决策都由**上游产物的约束集**驱动——规格先于资料、资料先于草稿。

- 写规格时只收作者口述，不拿外部作品倒推作者意志（参考档案是资料收集步的产物，不进规格步的读入表）。
- 采资料时以规格里的参考偏好为唯一分派依据。
- 进草稿步时，**规格就是全部创作决策的约束集**：其**根本属性（题材／基调等）是生成的硬约束**（写玄幻不出科幻大纲）。
- **实测标尺进生成**——作**量级感参照**（**心里的秤，不当要凑的数——不得为凑区间注水**）。
- **对照与三种结果判定不进生成**——那是大纲 v1 成形后（3.5 量级查）的**校验器**，治生成期 LLM 自己感觉不到的密度失衡（如百万字只对抗一两个敌人组织，生成完拿标尺一照才现形）。
- 生成期各处另持**方法论默认密度感**。

照常识「先多找参考再定写什么」在本域是反序——那会把本书写成参考书的仿品。

## 三、流程总览

### 1. 状态机与断点

当前阶段五值：**大纲-规格中/资料收集中/草稿中/打磨中/已完成**（粗枚举）。细定位靠**产物状态表**（每行＝步骤号＋产物＋状态，行序即时间序）。

断点续跑＝读产物状态表 → 报「你在哪」→ 从缺口续。

产物状态表由**阶段确认点与中途暂停确认点**的同批写入文件动作追加行（产物名/状态/备注）；其余步骤的④产物写入文件**不单独落行**（细定位靠产物自身）；另有明示落行的检查点（如 3.2／3.3 开工-接口验讫、量级查已处置）照各自条文落行。

格式＝三列表格（产物名｜状态｜备注），头部两行为「当前阶段」「产物状态表」标题——同批追加行，不由续跑时重建。

| 状态 | 子步骤 | 断点证据 | 续跑动作 |
| --- | --- | --- | --- |
| 规格中 | 1.0/1.1/1.2 | 目录与基本规格字段完备性/确认记录 | 读草稿→报缺项→续问；齐→重现门 |
| 资料收集中 | 2.1/2.2×N/2.3/2.4 | 档案头状态字段（只认档完）/标尺十二组齐/确认 | 缺哪本补派；标尺不齐重合成 |
| 草稿中 | 3.1→3.5 线性 | 大纲.md 各节完备性（含伏笔速记埋收）+候选选定标记+**底盘中途暂停确认**+人物卡逐张（主角/反派/配角） | 报「故事核齐、结构差 X 节」式定位→续 |
| 打磨中 | 4.1/4.2轮N/4.3/4.4 | 处方报告+处置记录/级联记录末链位/定格标记 | 有报告无处置→重现处置选项列表；从末链位续 |
| 已完成 | （终态） | 定格标记 | 修订不开新状态，走变更记录；移交卷纲域 |

**所有阶段确认点与中途暂停确认点**均须在产物状态表落行（同 §三.3 阶段确认点总表全项——规格门/资料门/底盘中途暂停/产物中途暂停×2/草稿验收/评委处置/轮末/自读/定稿门——落行式样如「世界观底盘-待确认」（**产物名列写产物名、状态列写状态值**；式样串是读法，不是单元格内容）），否则续跑不得跳过。

### 2. 产物依赖图

```text
基本规格.md（1.1 草稿 → 2.3 实测态 → 4.3 定格）
  ├→ 2.2 参考档案×N ─→ 2.3 参考标尺.md（独立，跨阶段）
  │                        └→ 3.5 量级查（大纲 v1 成形后十组对照——生成期作量级感参照读入）
  ├→ 3.1 故事核+金手指+主角 ─→ 3.2 体系+底盘 ─→ 3.3 结构+分卷+反派 ─→ 3.4 配角群像
  │     （伏笔速记：3.1/3.2 埋 ─→ 3.3 必读排位；反派卡 3.3 产、主角卡 3.1 产、配角卡 3.4 产——同模板）
  └→ 灵感库（外部）：3.x 拍四查询；产物「灵感来源」字段回流已用清单
```

### 3. 阶段确认点总表

| 阶段确认点 | 位置 | 过关条件 | 选项列表三选 |
| --- | --- | --- | --- |
| 规格门 | 1.2 | 联动校验+完备律+确认 | 确认/要改/先停 |
| 资料门 | 2.4 | 档案标尺过关+确认 | +看采风资料/要补采 |
| 底盘中途暂停 | 3.2 后 | 底盘过关+确认 | 确认搭主线/要改 |
| 草稿验收中途暂停 | 3.5 | 硬伤零+确认 | 进打磨/要改/先停 |
| 评委处置 | 4.1 每报告 | 条目全处置 | 按建议/部分/不改（登记） |
| 轮末 | 4.2 每轮 | 级联完整 | 再一轮/收工（上限 3） |
| 定稿门 | 4.4 | 收尾全过+确认 | 定稿/要改/先停 |
| **产物中途暂停** | 大纲.md 完成/人物卡群完成 | 产物完备+作者过目 | 有调整/继续 |
| 自读中途暂停 | 4.1.5 | 评委处置完 | 进循环/直接收工 |

三档与[skill资产标准](../../standards/skill资产标准.md) §6 的中途暂停点三级一一对应：

- 阶段确认点＝阶段级硬停（规格门／资料门／定稿门）。
- 中途暂停＝模块级暂停（底盘中途暂停／产物中途暂停×2／自读中途暂停）。
- 不阻断的通报点＝不阻塞的中间结果亮出（如各步②读入表自查、轮末通报）。

## 四、写法纪律

1. **步骤单元七字段**（序固定）：①作用〔一句大白话+一句心流锚〕②收集〔程序要干的事：读什么→取什么〕③怎么想④产物与完备校验⑤过关标准⑥交互〔逐字话术+{变量}占位〕⑦异常与降级。
2. **「怎么想」=思考指令**（问句链/因果指令），禁知识描述。生成型步骤走**四拍流水**：拍一·方法论问句 → 拍二·参考对照 → 拍三·量级参照 → 拍四·灵感注入；收集型步骤拍数递减。**四拍有权重**：每个生成型模块标**主拍**（本模块思考密度最高的一拍，问句从密），非主拍问句从简——四拍顺序不变，密度分配看模块性质。
3. **完备律（零待定推进）**：每步产物过关=必填字段全为具体值；「没想好」不停——现场给 2-3 个具体选项让作者选，或触发采风。比较想写的（灵感槽）除外。
4. **序号体系**：章中文序与步骤十位对齐；步骤 `N.M 步骤名`（名字为一级标识）；四拍定名；**阶段确认点命名制**（规格门/资料门/定稿门——阶段级硬停）区别于**中途暂停**（模块级条件停）。
5. **评委铁律**：三维度（编辑/作者/读者）苏格拉底问句，只呈报不拦截——处置权在作者，跳过须登记理由。
6. **数值纪律**：标尺类数值一律范围值+依据强度三级（锚定实测｜多源统计 N 源｜推演⚠️）；情绪用描述性文字，禁百分比。
7. **规格字段准入律**：只有「作者开书时真知道＋影响市场定位」的字段才进基本规格必答清单；创作决策类字段住创作步骤（有语境时再定），规格里**不设倾向类字段**——与「不收金手指/主角设定」同一原则的成文化。
8. **字段值必须可执行**：LLM 消费的字段，值要能直接转成写作/决策指令——基调等风格字段用**受控词表词组**（词表见附录 A-2，词序即主次（跨轴选词时为整体主次，同轴内为局部主次）），比例/百分比类伪精确数值不入字段（与第 6 条「情绪描述性」同源收紧）。
9. **统一情绪词表**：基调字段、参考标尺②情绪基调组、灵感库情绪爽点标签槽、评委问句的情绪判断、分卷五件套情绪弧线，五处共用[附录](大纲域设计-附录.md) A-2 词表——一处增词五处生效，查询对比才对齐。
10. **用户语言规范**（承旧 SKILL「话术零黑话」）：运行时对作者说的话一律大白话，内部术语只活在设计文档与文件层。对照表（不完整即增）——

    | 内部术语 | 用户话术 |
    | --- | --- |
    | 档案／档完 | 参考采风资料／采完了 |
    | 内核定标 | 这本书的本质类型 |
    | 锚定实测 | 实测（从原书数据数的） |
    | 多源统计 | 多方印证 |
    | 推演⚠️ | 推算值（仅供参考） |
    | 级联 | 相关检查 |
    | 完备律／零待定 | 信息齐了才往下走 |
    | 灵光位 | 顺手翻到的灵感 |
    | 标尺 | 标尺（作者原词，保留） |

11. **亲和簇原则**：耦合的思考放同一模块或相邻模块（**亲和簇内聚**），簇间通过**显式接口**把结论传递给下游（**接口传递**），避免注意力折返——跨模块来回跳会稀释思考密度、诱发返工。首例＝[草稿步](大纲域设计-草稿步.md)「模块序」节的模块重排：金手指-主角-故事核同簇入 3.1，下游按「金手指三型／体系+底盘」两接口消费。
12. **作家思维四问**（正式名「作家思维」）：生成型步骤开工前先跑四问（问句链，写在 ③怎么想 的**最前**）：
    - ①**理论选择**：这一步该用哪套写作理论/方法论？为什么是它而不是别的？
    - ②**知识缺口**：这件事上我有哪些是记不准、可能过时、样本偏的？
    - ③**权衡评估**：这里有几条路可走？各自的代价与最坏情况是什么？
    - ④**读者模拟**：读者读到这儿会怎么反应——哪里爽、哪里会弃读、哪里看不懂？

    **三级配档**（级别写在 `③怎么想` 行首的方头括号标记里，形如 `〔作家思维：全量〕`）：
    - **全量**＝四问全跑，用于产出创作决策的核心生成步骤（3.1／3.2／3.3／3.4——人设／冲突设计／大纲）。
    - **降级**＝只跑③权衡评估＋④读者模拟，用于采集、统计、评审、处方类（1.1／2.1／2.2／2.3／4.1／4.2）。
    - **免**＝确定性/机械步骤不跑（1.0／3.5／4.3）。

    **问与拍的关系**：四问是**开工前的前置自检**（回答「我该怎么想」），四拍是 ③怎么想 内部的问句流水（回答「这一步按什么顺序想」）；①的选定注入拍一，②的缺口影响拍二/三的对照计算方式。

    **写入位置（文件账本律）**：四问不是空跑自省，结论必须有文件写入位置。
    - ①理论选择：写进本步 ③怎么想 的首句。
    - ②知识缺口：逐条落成动作（触发采风／联网核实／问作者），**不得带缺口硬写**。
    - ③权衡与④读者模拟：结论写进本步产物的对应字段（3.1 故事核小节／3.2 体系条款／3.3 分卷五件套与节点表／3.4 人物卡）；产物无对应字段时写进该模块产物末尾的「已评估」备注。

    **④不替代评委**：读者模拟是**生成上下文内的自演**，与 4.1 读者评委的隔离素读（刻意不装标尺/规格/底盘）不是同一机制，不得用来省掉评委。

    **正例**＝3.1 故事核（全量四问）：①定「本书用哪套故事核模型」、②暴露「这个题材我没数」当场触发采风、③在「爽点前置 vs 悬念前置」间取舍、④定「第一章给不给金手指」；四条结论分别落进故事核小节与该模块的「已评估」备注。

    **反例**＝3.5 草稿验收跑全量四问：四查是机械校验，四问在此**没有产物写入位置**（书还没给读者看，「读者模拟」只能空跑；四查结论已有验收单模板承载）——正是「空跑思考」这条事故记录要防的。
13. **参考标尺用法**（作者定性：**参考而非硬指标——故事性大于硬指标**）：标尺治的病是**内容密度失衡**（百万字只有两三个反派、主要人物三四个——上千章的作品不该如此匮乏），不是配额考核。

    **十组二分**：
    - **范围组（5 组＋2 派生项）**＝定稿件已给区间、可由产物机械数出的组：①节奏目标（爆发密度/高潮周期 → 派生**节点数**）、②结构要点（暗线数/换图规划/条目数 → 派生**卷数**）、③人物分层名单、④反派层级、⑤伏笔量。
    - **视角组（5 组）**＝⑥情绪基调、⑦主角压力、⑧读者情绪曲线、⑨爽点类型分布、⑩信息揭示节奏——只作**参照视角**（**3.5 量级查时**对一对量级感觉），不做偏离判定。
    - **决策参照两组**：⑪结局形态分布／⑫感情线处理分布——形态选项、只给视角、不做偏离判定，由草稿步定案处消费。

    **三层分工**：脚本数事实（从产物机械数出范围组实测值）→ LLM 提处置（对照区间给建议）→ 作者裁决（**改标尺**与**红灯处置**须作者确认）。**本期无脚本**——范围组实测值由 LLM 代跑，须在产物中显式声明「本值系代跑」（标尺脚本属 skill 建设批次）。

    **三级判定**（实测值 n、标尺的区间记作 [a,b]；偏离幅度＝**所在侧边界值**（缺口侧取下限 a、超出侧取上限 b）÷3，向下取整、最小 1）：
    - **过**＝n∈[a,b]。
    - **说明**＝偏离＜幅度（在产物里写一句为什么即可，不阻塞）。
    - **须处置**＝偏离≥幅度。

    **记录用词**：判定为「须处置」后，产物状态表按四选之一记录为「**已处置**（补／删合／改标尺／接受并说明）」——「须处置」是判定态、「已处置」是写入文件态，两者不混用。

    **判定统一落 3.5 量级查**（大纲 v1 成形后**双对表**）：对标尺判量级（三种结果判定＋四选处置）、对照模板核对格判服从（违背约束**回改产物，不改规格**）。这是草稿期唯一定考点。

    3.x 各模块读入**实测标尺量级十组作量级感参照**（**心里的秤，不当要凑的数——不得为凑区间注水**），另持**方法论默认密度感**（撑体量／压节奏的常识量级）——**对照与三种结果判定不进生成期**（生成期对象未成形、无从对照：3.5 时一切已成形，两查集中于此）。

    **处置四选**（须处置时逐项选一，并落行产物状态表）：
    - **补**——按本书自身工艺补足，**禁止为凑区间注水**（新增反派须带层级定位与镜像点、新增节点须带因果与功能、新增伏笔须有埋有收）。
    - **删/合**——超出时收缩密度（合并功能位、拆卷出场、砍冗余节点）。
    - **改标尺**——本书体裁/体量与标尺本就不同（非修炼类、短卷结构、平台节奏差异），修正数值并注明依据强度，**须作者确认**（标尺是跨阶段资产，卷纲/正文继续对表）。
    - **接受并说明**——评估后认为偏离合理（如本书就是「少反派·深反派」结构），写明理由标「已评估·接受」。

    **反机械红线**：差异大时先答「本书是否真需要这个量级」，再决定补／改标尺／接受——**不得为对齐区间新增无功能内容**。

    **正例**：规格需 10 个反派、区间 15-20 → 缺口 5＝下限 15 的 1/3 → 须处置；评估后若本书确实只需 10 个，出口是「改标尺为 10-15」或「接受并说明」，**不必然补 5 个**。

    **反例**：看到缺 5 个就派 5 个只出场一次的杂鱼反派——密度指标过了，内容密度更差了。

## 五、域交互细则

立项域准入律的流程细则（§十 上线验收清单的「准入律」条为验收要点，本节为其流程细则）：

1. 三问段式：段一自由聊（对话式邀请在先，选项列表批随后——三个（选项列表）问句：投放平台／题材主类／篇幅量级，均可跳过）→ 段二回读默认（静默 + 回读）→ 段三对话式 ≤3（基调草案先行）。
2. **字段准入律**：**终局计算方式与感情线不在本阶段问**——决定点在草稿步；作者在段一主动说了的，**落 ⑤故事直觉**（本阶段不设该两项字段）。
3. 收拢式重问：段三未答全时，把剩余必答项合并成一次问清单重问，不逐项追问。
4. 过程性动作默认执行（如建结构）＋通报结果，不额外索取确认；有副作用的操作（写文件、调外部服务、删数据）才中途暂停确认。

三步式各段的话术、读入表与过关判定标准的逐字细则见[规格与资料步](大纲域设计-规格与资料步.md) 1.1；准入律字段清单见[附录](大纲域设计-附录.md) B-1。

## 六、评审体系总表

| 触发时点 | 检查 | 是否 spawn | 是否拦截 | 产物 |
| --- | --- | --- | --- | --- |
| 每步产物写入文件前 | 完备律（零待定清单） | 否 | 是（硬检查不过不算过关） | 步骤产物（完备校验随产物写入文件） |
| 草稿每模块 | 模块轻审三问 | 是（评委 subagent） | 否（只呈报） | 轻审记录（3.5 汇总入验收单） |
| 3.5 验收 | 四查全量 | 否 | 是（硬伤零才放行） | `打磨/草稿验收单.md` |
| 4.1 打磨入口 | 三维度全量审 | 是（三评委 subagent） | 否（只呈报不拦截） | `打磨/处方报告-R{N}.md` |
| 4.2 每轮 | 级联复审 | 是（受影响模块轻审 subagent） | 是（改动须工单确认后执行） | `打磨/级联记录.md` |
| 4.3 收尾 | 四查终验+孤儿检查 | 否 | 是（四查全过+孤儿清零才定格） | `打磨/定稿验收单.md` |

## 七、灵感库接口（可选外部资产）

灵感库是**可选外部资产**——作者跨项目的独立资产（独立 git、跨书、不随本仓分发），本域只在草稿步拍四消费它。本域对它的定位与接口形态四条：

1. **只消费不生产**：本域只按槽检索取条目，不同步更新、不维护——条目的增删改由作者在库侧手工做。
2. **初期状态（手动库）**：演进三步走——手动库 → 消费验证 → 触发信号成立后立项工具化；触发信号＝条目过百／同步更新需求成型／多书同时消费，任一成立才评估做成工具或技能。
3. **不依赖三条**：命中则用；未命中即跳过并如实通报；库不可用＝降级而非失败（拍四跳过，产物「灵感来源」字段标「库不可用」）。**任何判定标准不得引用它**——本域验收清单与各步过关条件里不出现「必须命中 N 条」类断言。
4. **契约可演进**：库的字段／交互／检索协议是**待消费验证的假设**，可能随使用演进；本域只认「库侧存在该契约」与接口形态，不认版本细节。契约修订由库侧自持（自持 git、自持版本号），本仓不做副本、不做镜像。

仓内只写接口形态：不复述契约内容、不写仓内契约路径、不复制库资产。

## 八、成本与降级

| 故障 | 降级 |
| --- | --- |
| 断网/超检索预算 | LLM 知识版档案（标推演⚠️）/等/换参考（作者在选项列表中选） |
| 无参考作品 | 2.2 跳过；标尺=方法论默认表（全组全推演⚠️） |
| 单参考 | 范围加宽+强度降档 |
| 灵感库不可用 | 拍四跳过+通报+字段标「库不可用」 |
| 评委 spawn 失败 | 主会话自跑三维度（降质通报） |
| 打磨超 3 轮 | 强制收尾进 4.3；超限后作者仍可手动再开一轮（记为第 4+轮，标注作者令） |

## 九、跑通信号（It's working if）

作者不开 skill 内部即可自查——出现这些信号说明本域跑通了：

- 阶段确认点与中途暂停停下来时，你说得出「现在在第几步、卡在哪个产物」，不必翻聊天记录回想。
- 关掉会话再敲 `/aether-outline`，它报出的位置和你记得的一样——断点从产物状态表读，不从对话记忆猜。
- 标尺亮红灯时你拿到的是**处置选项**（补／删合／改标尺／接受并说明），而不是一句「数字没达标」的判决。
- 大纲定稿后，每一卷的核心冲突句与卷末钩子你都说得出来；伏笔每条有埋有收，六节齐备。
- 三评委报告里出现「分歧点」而不是清一色同调——三位评委各执一双眼睛，不是同一个人说三遍。
- 采风断网或库不可用时流程照样往下走，且它明确告诉你「哪一处降级了」。

## 十、上线验收清单

域级条目（`/aether-outline` 装入目标项目后逐条核对）：

- 仅显式调用 `/aether-outline` 进入；入口判定按**文件证据**（无 `创作进度.md` → 全新开书）。
- 规格收集按三段式推进（段一自由聊（对话式邀请在先，选项列表批随后——三个（选项列表）问句 → 段二回读默认 → 段三对话式 ≤3（基调草案先行）），**终局与感情线不问**（准入律，细则见 §五「域交互细则」）。
- 规格门停等作者确认，**确认前不进入下一阶段**（阶段确认点＝阶段级硬停，与模块级中途暂停区分）。
- 交互模态标注与实况一致（封闭选择走选项列表、开放采集走对话式、过程性动作默认执行）。
- 不越阶段：阶段确认前不产出后续阶段实质内容（目标产物存在但为空模板即合规）。

作者验收基准（原话要点去编号后在册）：

- 作者验收基准：全书走**细纲党单路径**（不设粗纲／细纲双路径分叉）。
- 作者验收基准：每一步产物**信息齐了才往下走**（零待定推进）——同 §四 写法纪律 3。
- 作者验收基准：参考 mo-shu 时扬长避短——方法论是参考不是权威。
- 作者验收基准：动工前先出详细设计文档（本组五件即该文档），设计完备＋模板出件后才进建设。
- 作者验收基准：字段与枚举保持**开放集**（可自填组合，反复需要即增项入表）。
- 作者验收基准：评审机制与详细步骤齐备——四查 ＋ 子步骤表 ＋ 评审体系总表。

## 十一、开放项

- **草稿阶段分期建设（2026-09-19 作者裁定）**——一期＝框架与步骤（运行规格＋B-4/5/6/9 模板落 `assets/`＋进度接线＋3.5 量级查与原创对照**脚本**，**零 agent**：主生成流作者交互密集〔决定点三选／模块级确认／分叉暂停〕、亲和簇须同一上下文，不 agent 化；机械判定一律走脚本——脚本做确定性）；二期＝方法论萃取批（**天然 agent 批**——一源一路并行、萃取员规则文件＋萃取件形态断言做核验，复刻 web-scraper 模式）；三期＝评审面 agent（三评委等防污染评审，随 Stage 4 前夕建）。**agent 不预设计**——一期真机跑出实际痛点（上下文撑爆／需新鲜眼睛／并行 N 件）才按痛建，四条判定标准＝并行／上下文隔离／任务契约完备／可重复，占二才上；痛点记录落《施工问题记录》。
- 方法论萃取批**待启动**——纳入总表 6 项最高优先级方法论（情感线／冲突／节奏／非修炼类体系／人物设计／大纲结构）的萃取后置，施工期以过渡层问句为准（清单见[草稿步](大纲域设计-草稿步.md)「方法论纳入总表」节）。
- [附录](大纲域设计-附录.md) 的十件模板与统一情绪词表在域建设批落地时**已按分流位置**——部分留产品层、模板进 `assets/`、方法论与词表进 `references/`（**阶段一（规格步）资产已建**；B-2…B-10 随对应建设批）。
- 灵感库**工具化立项待触发**——触发信号（条目过百／同步更新需求成型／多书同时消费）任一成立才评估；契约演进归库侧。
- 库侧契约的「消费方」指针**待跨仓同步**——该指针按旧档名指认，需由作者在库侧仓库改为泛化措辞（外部资产只读，本仓不改）。
- 运行资产（skill 主文件、references、assets、脚本）随各批落地：**阶段一（规格步）资产已建**，其余随对应建设批；本组五件为设计定稿件，建设批不得偏离其判定标准。
- 草稿步「方法论纳入总表」的**萃取源提示列**体例待统一——`referFile 人物类+标尺压力组` 一处以「标尺XX组」为源（其余条用文件/网上形态）；**与「读入作量级感参照」不相抵**（该列语义＝萃取源，非读入指令），故本批不改；触发＝域建设批涉及该表时统一体例。
- **结构组提前照**——条目量／卷数／反派层级属结构位、v1 后才发现密度失衡的返工最痛（动 3.2/3.3 关键梁），是否在 3.3 成形时对结构组提前照一次——触发＝首批实机人工试跑发现 3.5 密度判定返工成本过高时评估。
- **统一修订工单机制**——任意时点（草稿期/打磨期/定稿后）修订走同构机制：提案（作者/LLM）→影响面（按 §三.2 依赖图倒推）→作者确认→执行＋级联标记→产物状态表落行；草稿期轻工单、打磨期即 4.2 工单、定稿后即变更记录——**写入位置＝卷纲域设计批升架构层（三域通用）**，本域记册。

### 常见问题

先收 0 条——真实来源（作者实际被问过、实际踩过的问题）目前为零，本节按「宁可少写也不注水」留位，随域建设批与首批实机人工试跑补录。
