# Changelog

本文件记录 Aether（原 Abzu，2026-09-19 更名）的显著变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循 [SemVer](https://semver.org/lang/zh-CN/)。

## 目录

- [Unreleased]

## [Unreleased]

### Added

- 施工问题记录＋事故表退役＋CHANGELOG 用法收窄（直改，作者裁定）：《施工机制》§八新增施工问题记录表（7 列＋填写规则 6 条＋沉淀方法 4 步，预填本会话 5 行实撞坑）；附录 §一 33 行事故记录表删除（有预防力的已内化为标准/检查器）、附录从 15K 缩至 3.2K（只剩规格道工具箱）；CHANGELOG 从「每批必落」收窄为「只落用户可感知变更（skill 新功能、行为变化、bug 修复），内部治理只落 git commit」。核验：`check.sh` 退出 0。

- 施工机制 §八「施工问题记录」（直改）：7 列表（日期/场景/类型/问题/修法/次数/等级）＋填写规则 6 条＋沉淀方法 4 步——踩坑立刻记一行，作者说「沉淀经验」时按类型＋场景聚类、判次数、提建议、裁定后写规则或建检查器；类型五枚举（命令/格式/流程/判断/引用）、等级三枚举（P0/P1/P2）；表里已预填本会话实撞的 5 行。核验：`check.sh` 退出 0。

- 《守卫缺口清单》整件退役（直改，作者裁定「有缺口就修或沉淀，别停中间态」）：46 条分发——已知守卫限制归守卫脚本自身注释、无事故出身的预写法删除、有事故出身的归入事故记录；`LEDGER_FILES` 同步收窄；全仓引用清零（附录 2 处历史标注保留）。核验：`check.sh` 退出 0。

- AGENTS §三 Windows 速查五条＋运行环境标准 §2 新增 4 条坑点（直改）：AGENTS 加「跑命令前先过这五行」——字节/字符陷阱、`grep -c` 算行不算次、管道吞退出码、进程替换不可靠、Python 调用差异；运行环境标准补齐 `wc -c` 字节口径、`<(...)` 不可靠、`python` 而非 `python3`、`sed -i` inode 风险（本会话实撞坑均源于此）。核验：`check.sh` 退出 0。

- 施工机制效率重构·独立审计整改（直改，事后审计位首跑）：只读审计三件得 16 项发现，全处置——恢复丢失条款 4 条（规则新增同步更新清单及「最后一层」终止句、§九≤规格 15%、成稿审查断言字面执行不得改写命令、施工 subagent 纪律归附录 §三）；订正过期计数（事故记录「其余 24 行」→25）；补悬空引用（#11 指向）；清本批自引入禁用词 7 处（台账×3／判据×3／止损／沉淀／交底×2／档位子串死角）。核验：`check.sh` 退出 0、三件用词零候选。

- 施工机制效率重构（直改，作者裁定极致提效）：**直改立为默认车道**（旧三车道并为两级——小改并为最轻情形注记）；**审查按风险分级**（默认无前置审查、作者看 diff；高风险批派单个只读事后审计、报告 ≤500 字符、不循环）；**核销改台账原则**（债跟主题走，规格批才设逐条确认表）；核验三步压两步（断言独立重跑限规格批）；成本註记直改批简化；subagent 施工细则归附录（规格道专用）。`施工机制` 12.4K → 5.5K（−55%）；`hooks/pre-commit` 核验为 4 行极简、不动。核验：`check.sh` 退出 0。

- 机架与插件分层（直改）：立**「（本项目＝…）」标记法**——开发侧定位通用机架、换 skill 项目整体复用，项目专有事实只住 `docs/product/`／`skills/`／带标记处；解耦 skill资产标准 6 处（域外休眠、域内准入、七字段单元、产物接口指针等）、文字与命名标准 §8 产品词行标记为项目插件、运行环境标准 §9／§10 通用化（站点边界与用户数据形态改「通则＋本项目例」）；守卫缺口清单登记 G45（机架通用性检查，触发＝复用于第二个项目）。核验：`check.sh` 退出 0。

- 产品层共享五件评审＋用词批 3 清零（直改）：五件 11 处悬空引用修复（测试与验收／触发与交互／命名／术语／安全与合规→新址）；产品层＋skills 禁用词迁移 655 处（预映射 阶段门控／命令薄壳／域壳／公开样本锚点 等复合词）＋冗余复合收口 10 处；§8 登记 4 个产品领域词（标尺／底盘／处方／定格）并立守卫豁免（`check_content` 先遮盖后计数）；SKILL.md 固定节名随迁「阶段确认点与交互原则」、`check.sh` [2] 段期望清单同步。用词存量 678 → **0**。核验：`check.sh` 退出 0。

- skill资产标准瘦身（直改）：§14 内容主张规范（情态三级／数字标来源／占位符）迁《文字与命名标准》§17——措辞力度法随「怎么写文」归家，原位留指针；§3 调用控制键实测记录压缩（四要素细节指归 `archive/087`）；头注立拆分触发条件（触顶 15K 预算或第二域落地细则膨胀，届时按「壳与触发／资产文件与交互」两件拆）。11.2K → 10.9K。核验：`check.sh` 退出 0。

- 《版本与分发标准》整件退役（直改，作者裁定无实际意义）：活法三处迁移——CHANGELOG 条目限长 400 字符的唯一出处转 `施工机制` §三（`check_content` 真源指针同改）、用户书稿保护转 `运行环境标准` §10 并转正为条文、SemVer 版本语义入 `skill资产标准` §3；发布流程六步等从未历实例的预写法整体撤销，随首个发布批再立法。《运行环境标准》§9 五条压两条（超时／降级与 §5 重复，删）。核验：`check.sh` 退出 0（含一处 MD038 自撞修复）。

- 开发侧文件逐件评审整改（直改）：AGENTS 双路由表合一（任务／区域两张并一张）、§七 两处断链改指（产品层信号→文字与命名标准 §16、数字可复算→附录 §三）、「解冻条件」重复订正；skill资产标准去英文残留（提前收尾冲动／评估闭环三步名）＋§15 与运行环境标准 §5 三行重叠改指针；产物契约标准出处压缩＋《命名标准》断链改指；版本与分发标准「已建skill 主文件」×5 订正＋可跑校验段压缩。核验：`check.sh` 退出 0、六件对已删件零引用。

- 开发侧基础设施重构第二批（作者授权直改）：标准件 12→5——skill形态＋触发与交互合为《skill资产标准》；命名＋用词＋术语＋文档写作合为《文字与命名标准》（禁用清单界标段随迁、`WORD_SRC` 改读）；安全与合规并入《运行环境标准》；测试与验收溶解（段现状表删、红起点与分档入 `施工机制` §四、G 表→新冷件 `守卫缺口清单.md`）；规格写作标准拆入施工机制与附录 §三（自审五条＋断言写法 14 条）；施工机制立双车道（`直改`／`小改`／主线）并压缩 §四 同步更新清单；`check_spec` 补 G40 兜底＋清单全集收新冷件。核验：`check.sh` 退出 0。

- 开发侧基础设施重构（作者授权直改，无规格件）：用词迁移批 1＋2 落地——`AGENTS.md`／`施工机制` 与附录／12 份标准件替换禁用词 897 处并手工修复坏复合词约 40 处（清单自相矛盾处「判三」改「同步更新文件核对」）；`AGENTS.md` 新增 §八 模型执行通用准则＋路由表补 4 行；`施工机制` 立「四步主线总览」、修 §八 模板悬空引用（模板实居附录 §三）；`check_spec.py` 四处判据新旧双读（本批核销／现状锚点／自由度分布／回写面对账）。核验：`check.sh` 退出 0。

- 审计余项落台账（108）：`107` 审计余 20 条落长期台账——`施工机制` §七 补 pathspec 与未跟踪件句、§五 补归档后补记例外、§八 表注与 §七 归档件实测值句；`测试与验收标准` §4 补 G38–G42 五行并补 G1／G27／G31；`文档写作标准` §3 补 10.／11.；`术语标准` §二 与 `产物契约标准` §2 各补一条；`patch_file.py` 头注补三坑；`check_spec.py` 删死代码 COST_WORDS；`105` §七 **三行**改标、`103` **四处**改标与 §七 汇总同步。核验：`check.sh` 退出 0、断言 1–8 实跑。（往返 176／<1h）
- 施工机制效率重构（直改，无规格件）：`施工机制` §二 **「快」行退役**（近 14 批头注实测 0 次使用、仅 `105` 一次误标）＋检查 G「路径定级」判据下线＋`G30` 销号；§四 回写清单立**「回写面对账」表**（触检查面须逐面表态），检查 E 加**判三**核其存在性；§三 立**审查记录限长**（每轮一行 ≤250 字符、`§九` ≤规格 15%，`109` 实测 30%）；交底装载改按节点名（`§二`／`§三`／`§五`）＋装载量口径复算（字符口径 ≈10.7 万/实例）；内容轨新增**热路径字数预算**（`AGENTS.md` 8000／`施工机制` 24000 冻结线／标准件 15000 字符）；`AGENTS.md` §七 A-I→A–J。核验：`check.sh` 退出 0、lint 0 issue／81 件。（直改／<1h；无施工段与审查段）
- 施工机制效率重构 T2（直改，无规格件）：**冷热分离**——反模式清单全文／缺口表／三份模板／立例依据迁入新件 `施工机制-附录.md`（台账全集同步纳入该件），热路径由 **23,112 → 12,240 字符（−47%）**；§二 **四路径 → 一条主线 ＋ 规模开关**（`小改`），**档位改由检查 E 判四按清单实算**、与头注比对（自述只作声明）；交底收敛为**固定 4 项**；`AGENTS.md` 补「改某区域前先读什么」表；热路径字数预算收紧至 13,000。核验：`check.sh` 退出 0、lint 0 issue／82 件。（直改／<2h；无施工段与审查段）
- 施工机制效率重构 T3–T5b（直改，无规格件）：审查 **3 位 → 2 位**（窄域复审并入产物审查的**第一段**，代理实例 ≤4）＋**审查报告限长 ≤2,000 字符**；**T4** 核销写法收口（§七 只写五档＋落点）；**T5-1** 新增 `check_spec --reconcile`（实盘 diff ↔ §二 声明面，核验第 1 步件级机械化）并与 `--verify` 合一；**T5-2** 提交前自检四项标注机械覆盖；**T5-3** 预警行追加分类计数；**T5b** `edits.json` tasks 层约定。热路径 12,240 → 11,997。核验：`check.sh` 退出 0、lint 0 issue／82 件。（直改／<2h；无施工段与审查段）
- 用词规范与禁用清单（111）：新增 `用词标准`（用词规则六条＋禁用清单 82 词、唯一出处）＋`check_content.py` 加第 ⑦ 项用词候选检查（恒不置红）＋`check.sh` 与两标准件同步；存量迁移分三批（见该件 §一.3）。核验：`check.sh` 退出 0、断言 1–20 实跑。（往返 约 245／<2h；规划段 约 140｜成稿审查段 2｜施工段 79｜产物审查段 约 24｜合计 约 245）

- 机制产物审核整改（110）：`施工机制-附录` §三 规格模板 §九 行删去已退役的「快路径批次（免审查）」子句、行内核验注记移出模板行落 §三 节末散文；`测试与验收标准` §4 `G30` 状态格改标已销号、`G42` 三处已下线判据引用与「产物审查」计数（8→7）订正。核验：`check.sh` 退出 0、断言 1–11 实跑。（往返 154／<1h；规划段 47｜成稿审查段 21｜产物审查段 20｜施工段 66｜合计 154）
- 闸门降噪三事（109）：check_spec.py 治「带语法的文本被当字面文本」三处——检查 C 加粗归一两侧对称（G37 假阳除根）＋检查 A 对含正则元字符的 token 三态分流（剥 ^ 按行首计／余者出「口径不可判」，不再谎报「现行为 0」）＋检查 C 对未命中引号块到件外集检索、命中者追加「疑现状侧引文…成因待判」（#33 部分机械化）；施工机制 §八 §九 模板行与 :75 立「R 须分段列（含合计）」、§六 #24／#33 回填本批实例（含频次与分项同步）；测试与验收标准 增 G43、G37 转本批处置；105／103 三处反模式计数订正为 33；check.sh 两处枚举同步。核验：check.sh 退出 0、断言 1–6 实跑。（往返 360／1-4h；规划段 159｜施工段 113｜成稿审查段 33｜窄域复审段 23｜产物审查段 32｜合计 360）

- 归档前经验沉淀闸（107）：`施工机制` §三 立「移动前逐条判沉淀」闸（未逐条写出落点者不得移入 archive）＋§八 核销写法补「已沉淀须点名台账条号」＋§七 与 §八 模板两处改指 §三；`规格写作标准` §6 归档要求改指针、§5 补第 14 条；`运行环境标准` §一.2 补第 8 项（`shell=True` 须声明设计宿主与实测宿主）；`命名标准` §5 定名「随对应阶段批」；`测试与验收标准` §4 补 G32–G36；`check_spec.py` 检查 J／H 各扩台账归宿判据＋§九 成本字段判据；`check.sh` 与两表回写面同步。核验：`check.sh` 退出 0、断言 1–8 实跑。（往返 270／<1h）
- 定级守卫与自述即无效（106）：检查 G 加「路径定级」判据（头注「路径判定」行首个加粗片段含「快」而 §二 含 [B]／[C]／混合行即出候选；判定置于 `head` 早退之前，头注缺分布行时仍生效；汇总行补「定级 快／非快／未写」）＋`规格写作标准` §4 立第 5 条「自述有守卫」（自审四条→五条，真源）＋`施工机制` :52 指针／§四 模板／§二 前导语与快行守门 bullet 同步、§六 新增 `#31`＋`测试与验收标准` 补 `G30` 并回写 §1.1 与 §三。核验：`check.sh` 退出 0、断言 1–8 实跑。（往返 129／<1h）
- 三处缺口补齐（105）：检查 G 认 `[C]` 档（`check_freedom()` 清单侧加 `c_c`，`fmt()` 与不符判据各加 `[C]×c` 一格；docstring 同步改述；不新增常量）＋`测试与验收标准` §4 `G29` 转「本批处置」＋`施工机制` §六 `#29` 补机械判据半句。**另自查出「快路径」定级失守**——头注自称快路径而 §二 有 `[B]` 项、两轮审查被免；已如实改记标准路径、补做成稿审查（findings 7：P0:1 P1:2 P2:4）、守卫与通用条文转 `106` 批。核验：`check.sh` 退出 0、断言 1–4 实跑。（往返 100／<1h）

- 快路径与成本可见面（103）：施工机制 §二 新增「快」行（三条触发条件缺一即回退＋混装按最重项定级）＋§五 两处送审例外；§八 审查记录行与 §三 `CHANGELOG` 项各补成本字段（工具往返数／周期时长）；四处「三条路径」同步改「四条路径」。**用词闸（内容轨⑥疑似自造词）经施工期实测后撤销**——中文无词边界，n-gram 判据伪阳 5.4 万条不可用，且其运行期入参件会使干净检出的门禁恒红；`术语标准` §三 两行回「待建」并记因。核验：`check.sh` 退出 0、`内容轨` 候选 0；断言 1–5 实跑。（往返 106／1-4h）

- 上下文成本与账本纪律（102）：施工机制拆「实跑纪律（批式）」为规则／理由／适用范围三条并扩辖审查与核验位（所读文件清单）、【核验请求】模板五→六项（工具往返数）、立「审查记录写法三则」与「同批落不得留占位符」、#28 补第四半句与两实例、缺口表补 协2；`check_spec.py` 新增检查 J（审查记录节存在性与 N 逐条对账，回写面七处）、`check.sh` 头注与 [4] 段 echo 补检查J、`测试与验收标准` 补 G28（4 格行）＋G27 行裸竖线改全角占位、两处 [4] 行。核验：check.sh 退出 0；断言 1–6 实跑。
- 外审第三轮收口（101）：外审三轮 19 项裁定全落——施工机制（预立法校准／删 git add -N 次选／轻行对齐真源＋AGENTS 豁免／自审四条归一真源／补 git 历史腿与批号句／#28 计数三半句／裸点名 16 处——`施工机制` 13＋`AGENTS.md` 3）、标准件（三要素对齐／旧仓限定／G 表四列化＋G19 归位＋销号注＋行号改函数名／壳份数对齐／入口措辞对齐／预立法例句归位）、skill 资产四件（模板读法消歧／假降级先换模型重试／前向引用标注／完备豁免／变体标记）、check_content 跳跨度与围栏、check.sh [4] 段加检查号、.gitignore 两临时模式、check_spec 格数行入预警计数。核验：check.sh 退出 0；断言 1–7 逐条实跑（6① 排除自指回声行后真候选 0）；F21/F26 夹具自证随核验请求。
- 规格自检器增检查 I（轻路径，无规格）：`scripts/check_spec.py` 加**锚点对源核验**——§一「现状锚点」节的 文件:行号 引用 ↔ 目标件实况（判据：行号超界／指向空行／引文不在±3 行内——全件搜索给出实际行号；引文归一化剥 ` 与 *、省略号分段匹配）。事故出身：098／099／100 三批成稿审查的锚点 P0 族（100-P0-1 全表行号为前批落地前旧号，两轮 subagent 合计约 3.4M token）。回写：check.sh 头注与 echo、测试与验收标准 §1.1／§三、AGENTS.md §七（A-H→A-I）。核验：合成夹具五型全中（正确锚点静默）；归档件上漂移均给出真行号。
- AGENTS.md 瘦身补链（轻路径）：§四 速查索引各条**补解冻触发关键词**（13 条，取自 git 历史原版本的解冻条件原文）＋「见本节原文」悬空指改指 **git 历史原版本（`578815a` 及此前）**；红线 1 补回两句裁定明细（fetch 等在内／授权不外延）；§七 补治理/产品信号不得互引句；同步改 `运行环境标准` §44 与 `文档写作标准` §82 的两处外部指向、§74 引文随现表头订正。核验：`bash scripts/check.sh` 全绿；7+2 处经 `patch_file.py` 四段落盘（形态闸实战首跑）。
- AGENTS.md 治理瘦身批（外审高优 5 项全采纳）：红线 1 表格化/路由去展开/红线 4·6 去重外链/权威顺序例外简化/自检信号表格化/不做清单改速查索引（方案 A 压缩，真源不外移）/头注压缩。83→69 行（-17%）。

- 字段移植与形态收口（099）：走查二轮六发现＋作者三裁落完——书名移出 1.1（五件归零，新落点＝草稿步 3.1 故事核＋B-4/3.1⑤/B-9）＋占位符「参考-{书名}」改「{参考作品名}」；「避雷」改「作者绝对不写（对话式）/作者主观禁区清单」；触发与交互标准 §3 第 7 条收窄「同一屏只出一种模态」＋产品层四处同屏句改分模态批；门控改「在对话内贴出产物全文」；模板「约束」节补原样保留声明＋产物实例化纪律；底色交织改「段二可按段一推导补」；1.1⑤ 补「产物对规」；落行式样补列位说明；卷数改「实测回填的派生栏」；标尺进草稿步作量级感参照；3.5 扩双对表。核验：check.sh 全绿、lint 0、检查 G 不符 0、断言 1b–6 实跑（1a 附录计数与 F4 落点相抵，已呈报裁定）。
- 采集与字段面收口（098）：走查发现七条落完——1.1 段一改「邀请＋同屏三问（选项列表，均可跳过）」、段三基调改草案先行（词表取值规则补「不得以味道相克质疑底色交织」）；`绝对不写`→`作者绝对不写`、删 `读者避雷点` 与两倾向字段（准入律改述：不在本阶段问、原话落⑤故事直觉）、`读者画像三件` 并入 `读者定位`；产物状态表落行口径消歧（只门控／停靠落行）；标尺增「决策参照两组」（⑪结局形态分布／⑫感情线处理分布）＋档案 B-2 补两字段＋草稿步装载开例外＋命名「十组」→「十二组」（量级口径保留）；`触发与交互标准` §3 立第 7 条（一条消息可并列多问句、各句各自标模态）。＋三轮审查 findings 全落（F23–F30）。核验：check.sh 全绿、lint 0 issue、检查 G 不符 0 项、断言 1–5 实跑。
- 修订工具批（091）：新增 `scripts/patch_file.py`（修订工具，三段式：全量校验 → 统一写盘 → 回读核验；锚点由磁盘校验、命中 0 或 >1 即中止并打印实际命中行；支持 `--set`／`--append`／`--insert-before`／`--batch`；UTF-8 无 BOM 且保持目标件行尾）＋`施工机制` §三 立「修订工具律」与 §六 反模式 #20（取代 #18 的手写脚本路径）＋`命名标准` §2「仓级开发工具」行与 §1 目录树／表注同步＋`测试与验收标准` G20 触发列改述（091 复核判残余不存在）。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175／断链 0、断言 1–8＋3b／3c 逐条实跑。

- 施工效率批（089）：五条效率纪律落流程真源（实跑一律批式；必读输入按「文件:节」点名、未点名不读；施工 subagent 一律后台 spawn；机器已覆盖面审查不重复；小改动攒批）＋规格自审三条→四条（新增「范围与期望同源」）＋`check_spec.py` 新增检查 F（禁改面禁词 ↔ 改动清单目标侧引号块，候选非阻断）＋`规格写作标准` §5 第 13 条与 §三 锚点两行＋`测试与验收标准` `[4]` 枚举补全为六项。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175／断链 0、断言 1–11 逐条实跑。

- 阶段一资产与降级纪律（088）：大纲域四件运行资产落地（域工作流／Stage 1 阶段文件／统一情绪词表／基本规格模板）；建设状态通报归位域工作流（壳与阶段文件只引用不复制）＋壳补降级纪律与跨域意图告知；087 三条账落真源（未建阶段如实告知入 `触发与交互标准` §2、装后重载窗口入 `版本与分发标准` §3／§7、走查环境前提入 `测试与验收标准` §3）；`书目录契约` 与产品层状态句同批改述（阶段一交付口径）。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175／断链 0、断言 1–12 逐条实跑；**真机走查（断言 11）待作者执行**。

- 最小壳与真机试验（087）：首个 skill 壳落地（`skills/abzu-outline/SKILL.md`——四节骨架／五键／建设状态如实告知段）；`check.sh` 启用 `[2]` 段（纯本地断言：逐壳六项＋段行一条，零外部依赖）；各处「未建」状态真源同步（产品层 6 处／外部 2 处／标准件 12 处）；真机走查（作者，Claude Code 2.1.270）：菜单重载后可见、该键经机制实证抑制自主触发、宿主接受而参考校验器拒收——四要素回写 `skill形态标准` §3、联订 `架构设计` §二.2；空目录端到端跑通（入口判定→状态行→不产占位）。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175／断链 0、断言 1–12 逐条实跑（两处偏差已呈报并据裁定订正规格：4② 补 `#`／5 限 `[2]` 段内）。

### Changed

- 项目更名 Abzu → **Aether**（作者裁定 2026-09-19）：GitHub 仓库同步改名（`Chained1001/Aether`），安装命令改为 `npx skills add Chained1001/Aether -y`；宪法项目一句、README、产品定义、架构设计、SKILL 描述、验收场景、命名标准的目录树同步换名；**skill 前缀同批全换**（`abzu-outline` → `aether-outline`，目录 git mv、调用改 `/aether-outline`、卷纲正文占位域同步 `aether-volume`／`aether-write`——用户须重装 skill 并重启 Claude Code）。本地文件夹名待作者会话收尾后手动改。
- 不做清单解冻并销项：**doc-budget 字数预算**（每批必读件字数预算）——`check.sh` [3] 段与 `scripts/check_content.py` 的 `_check_budget()` 已实现并常跑，冻结条件自破；作者裁定随批 117 （`docs/specs/117-2026-09-17-审计修复与脚本瘦身批.md`）规格确认作出，AGENTS §四 不做清单同步删项。

- 修订工具形态闸修复（轻路径）：`patch_file.py` 的形态闸补显式 `encoding`——原 `subprocess.run(text=True)` 在 Windows 默认 GBK 解码 markdownlint 的 UTF-8 输出，崩在取 `returncode` 前，致闸恒判未过、明细被吞。修后正例放行、反例被拦并给明细、零写盘。核验：`check.sh` 全绿。
- 归档件授权更正（2026-09-16）：`docs/specs/archive/099-…md:244` 的 §七 汇总行由「仍待 8｜＝22」更正为「**仍待 7｜＝21**」（表体实 21 行）——由新立的**检查 H**（§七 汇总 ↔ 表体逐格对账）查出，**经作者授权**，属「归档件只读」的明文例外（同 `archive/071` 链接修正先例）。核验：`check_spec.py --static` 对该件报「检查 H 不符 0 项」。
- 修订工具加形态闸（轻路径，无规格）：`scripts/patch_file.py` 三段式 → **四段式**（新增第②段**形态闸**：`.md` 目标件对**待写入全文**跑 `markdownlint-cli2`，不过即中止——闸在写盘**之前**，「不过＝零写盘」不被破坏；`--no-lint` 逃逸；`npx` 不可用或该件被 ignore 排除时**明示提示**而非静默通过）。事故出身：规划方对 `.md` 落盘三次同型踩 MD038。核验：正例通过、反例被拦且零写盘。
- 规格自检器增检查 H（轻路径，无规格）：`scripts/check_spec.py` 加**§七 核销表汇总行 ↔ 表体状态列**逐格对账＋**五档外状态词**判（事故出身：规划方三次同型写错该汇总行）；回写 `check.sh` 头注与 echo 的 [4] 段枚举、`测试与验收标准` §1.1／§三、`AGENTS.md` §七（七类→八类）、`施工机制` §三／§六 `#13`／`命名标准` §1 目录树注。核验：正例 0 不符、反例精准命中；并在归档件上查出 `099` 一处真阳性。
- 规格字段清账与工具面收口（100）：`卷数` 移出规格、只住标尺（删 `assets` 四处＋`规格与资料步`／`stage-spec`／`附录` 同构处，三处改述为「参考标尺」）＋`每章字数` 删除（7 处落点并连带平台常量 4 处）＋草稿步四处失效「⑬⑭」括注删净＋「四规则」两处改「三规则」；`scripts/` 工具面收口（`check.sh` 依赖行补 `[5]`／批次引用三分处置并四件文档头补口径／`G20` 台账订正／`测试与验收标准` §4 补登 `G26`／`G27`）。核验：`bash scripts/check.sh` 全绿、断言 1–5 逐条实跑。
- 反模式回填（轻路径，无规格）：`施工机制` §六 反模式清单新增 `#27`——「代码跨度内容以反斜杠收尾」（收尾反引号被转义、跨度误配对吞掉后文；判据已由检查 D ② 机械化）；正确方式＝跨度内路径不留尾反斜杠，含路径的长跨度写完即跑门禁核对。出处：2026-09-15 `094` 走查记录回填时实撞（3 条预警、已修）。核验：`bash scripts/check.sh` 全绿、lint 0 issue。
- 反模式回填（轻路径，无规格）：`施工机制` §六 反模式清单新增 `#26`——「拿世界知识的浅层直觉当设计判据」；正确方式＝先查该张力在既有设计里有无承载层（本例：基调的底色交织层 ＋ 卷纲域的分卷情绪弧线），有承载层则张力是常态。出处：2026-09-15 真机走查 1.1 段三 基调（实机与规划方同型误判）。核验：`bash scripts/check.sh` 全绿、lint 0 issue。
- 文档形态与工具一致（097）：`skill形态标准` 四处形态收口（批次叙事改状态陈述／删过程数字／§13 表头注补机检列口径／`[3]` 段头注补加粗密度）＋`AGENTS.md` 六处（头注补至 8 项／§七 第二条主体与注同向且四信号平行／§四 末句并列改破折号／红线 1 括注拆为同向两段／红线 4 句式消歧）＋工具层三处（`MD_LINK` 同步为认 title 形／`check.sh` 头注声明 `set -u` 哨兵契约／`版本与分发标准` §3 显性化条目格式）＋回写面扩为五处并并列「缺口表」＋反模式 `#21` 并入 `G22` 族第五次复发＋`命名标准` §1 补「预留 vs 预立法」分野句。核验：`bash scripts/check.sh` 全绿、lint 0 issue、断链 0（新增 3 条归档件示例串候选属预期）、断言 1–7 逐条实跑。
- 规则回写面（096）：处置三份评审公共发现「新规立完、射程面没被回扫」——`AGENTS.md` §六.3 语义消歧（作者裁定意图 A：指定越界／两处指定互抵均走升级）＋`施工机制` §四 回写清单扩为四处（补 `check.sh` 头注的段行与段名）并并列新增「规则新增回写清单」（含最后一层终止句）＋`规格写作标准` §8 补执行面与存量清账口径＋7 处预立法标记逐条补「关联实例判据」＋`施工机制` §六 `#9` 回填本轮两笔事故。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接 176／断链 0、断言 1–6 逐条实跑。
- 施工交底装载路径（轻路径，无规格）：`施工机制` §八 交底七项之 6 补「凡本批触及 `skills/` 资产正文者，默认必带 `skill形态标准` §6（结构真源）与 §13（类别索引）」——治「规范存在但不被引到」：四段式／三要素／七字段单元／头部声明这些形态规范此前从未进过任何交底，全靠规划方复述（复述有损）。核验：`bash scripts/check.sh` 全绿、lint 0 issue。

- 守卫回写（095）：处置外审 findings——三项 P1（守卫变更未回写上层文档）同族收口：`AGENTS.md` §七 状态句改动态指认（引用闭合已由 `[3]` 段常驻检查）＋§七 第三条与 §二 路由表枚举同步；`测试与验收标准` §1.1 `[4]`／`[2]` 行与 §三 锚点补「自由度分布核对」「⑦段行校验」、G22 补 §1.1 回写核销注；`check_content.py` 头注 ⑤ 补至五词；`skill形态标准` §9.3 明示两行为必含项；`施工机制` §四 立「守卫变更回写清单」＋§六 反模式回填 #21–#25；`规格写作标准` §8 立预立法到期机制（关联实例判据）；`check.sh` 收尾行补已检范围。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接 176／断链 0、断言 1–6 逐条实跑。
- 阶段一资产内容（094）：大纲域 Stage 1 资产的自包含收口——1.2 规格门末句补落行式样（`{产物名}-待确认`＋具名例，单一真源，壳与工作流不复制）＋出处行裸具名《大纲域设计·规格与资料步》改述为「产品层设计件（维护出处：`大纲域设计-规格与资料步` §一；运行时不必加载）」＋8 行「随 089–091」批次号型标记按到期制改述为「随对应阶段批」（不改「未建」的事实陈述）。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接 176／断链 0、断言 1–6 逐条实跑；**真机走查（作者，2026-09-15）：1.0→1.1→1.2 走通、无阻断；判据①（落行式样）＝出现；走查发现七条已录本规格 §九，处置归 `098` 批。**
- 守卫面建设（093）：`[3]` 内容轨与 `[5]` 脚本语法两段落地（新建 `scripts/check_content.py`——引用闭合／TOC／行数与条目限长／「维护出处」标注五检，分档见 `测试与验收标准` §1.2）＋`check_spec.py` 认转义竖线（`_cells()` 按未转义竖线切分并还原）＋格数校验（`_change_rows()`，候选非阻断）＋新增检查 G（头注「自由度分布」↔ 改动清单自由度列）＋`CHANGELOG` 条目限长真源入 `版本与分发标准` §3＋`[5]` 范围四载体收口＋同族「待建」状态行九处收口。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接 176／断链 0、断言 1–6 逐条实跑。

- 真源面收口（092）：`命名标准` §5 立「运行时资产引用治理文档」口径（禁裸指向：可执行事实须自包含、追溯出处须带「维护出处／运行时不必加载」标注）＋§5 表补正例行＋§9.1 加指引半句＋§2 行名「阶段方法论」统一为「阶段文件」＋`skill形态标准` §13 矩阵补「阶段文件」「仓级开发工具」两行＋`skills/abzu-outline/` 内 4 处指针加标注（仅加标注，零行为指令变动；**标注词定名「维护出处」——避与产品层「设计真源」同形异义**）＋`AGENTS.md` 两处状态句改述（作者确认）＋`skill形态标准` §12.4 补真机走查豁免指引。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 176／断链 0、断言 1–6 逐条实跑。

- 治理收口（090）：G20（守卫与工具文档的批次叙事收敛）——`check_spec.py` docstring 的批号 3 处与「本批…」裁定括注 2 处降为指向式（技术承载逐字不动，顶部加事故出身与历次裁定索引行）＋`check.sh` `[2]` 段 ⑦ 段注改述与 `[4]` 段标题行枚举补全为六项（`:10` 头注与 `:111` echo 两处同口径）；G21（数字来源与依据强度）——`安全与合规标准` §4.1 限速值补〔自定〕与可覆盖声明（§三 锚点同步）＋`运行环境标准` §2.6 重排（规范句前置、本机 ugrep 实测降为出处注内的辅助事实）；另 `测试与验收标准` §4 新增 G22。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175／断链 0、断言 1–8 逐条实跑。

- 反模式回填与口径补述（轻路径，无规格）：`施工机制` §六 反模式清单回填 7 条（代码跨度边缘空格／表格单元裸竖线／列表前插顶翻序号／表格单元内联 HTML／中文用非仓内路径与按字节切工具／凭记忆写匹配键／截断或放宽判据下结论，各附事故出处）＋ §三 核销机制补「本批内已处置的开放项须同批改标」＋ §一 第 2 条补句号（089 §九 曾判「不改」，据外部评审改判）；`规格写作标准` §5.8 补计数口径（计行／计出现次数／计文件数）须逐条标注；`测试与验收标准` §1.2 澄清 [4] 段置红只指环境缺失或工具崩溃（静态发现恒只呈报）、§4 登记 G20（守卫与工具文档的批次叙事收敛）与 G21（两处标准件的数字来源与依据强度）。核验：`bash scripts/check.sh` 全绿、lint 0 issue。

- skill 资产结构对齐（086）：资产结构由「目录三件」对齐为 Agent Skills 标准形态——引入 `assets/`（模板与资源）、模板从 `references/` 迁 `assets/`、`references` 平铺去 `{skill 名}/` 子目录层、分流判据（被读的说明归 `references/`、被填的骨架归 `assets/`）落 §1、修「三件」三处自相抵（§1／条目 3／机检锚点）、`术语标准` 的 skill 资产定义同步；改动 17 处／3 件标准件（`skill形态标准` 8／`命名标准` 8／`术语标准` 1），本批只对齐规则、不建 `skills/`。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175 断链 0、断言 1-9 全过、`git status` 5 行无 `skills/`。

- 标尺时序重定义（085）：约束集收窄——进草稿步时规格即全部创作决策的约束集（其根本属性为生成的硬约束），**实测标尺不进生成**（移出装载层，改作大纲 v1 成形后的校验器），治生成期 LLM 感觉不到的密度失衡；实测对照与三态判定统一集中到 3.5 量级查（草稿期唯一定考点），②收集补装对照原料 `参考标尺.md`；改动覆盖装载表 4 行／过关／停靠／依赖图／2.3 分装句／附录默认值全层；开放项追加结构组提前照、统一修订工单机制两条。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175 断链 0、归档零改动、断言 1-9 全过。

- 断言 token 反转义与写作律（083）：`check_spec.py` 的 `_assert_refs()` 对 token 三步反转义（markdown 表格转义形 `` \` ``→`` ` ``、`\[`→`[`、`\]`→`]`；`\.`／`\|` 属 regex 语义、不反转义），消除「目标文本未提及」类假候选；`规格写作标准` §5 增第 12 条（断言目标件须写具体文件路径——聚合写法使零命中核对静默失守）。核验：`check.sh` 全绿、lint 0 issue、链接一行式 178 断链 0、归档零改动；冻结语料静态差异面恰五类（消失假候选 15、新增自噬 7、中性汇总 ±4 行、净化后仍候选 6、跳过行净化 1）、三态 305→303。

- 评审收口微调（084，轻路径）：`check_spec.py` 检查 E 的 docstring 补「E 为空即跳过」定案句（取舍为有意——信号由汇总行「无补集式断言」字段逐件承载，终结第九轮评审五轮拉锯的 P2-2；根因＝驳回记录的载体在归档规格、评审文件集不可见，定案句落判据所在处）；token 反转义注释的 `\|` 措辞精确化（BRE 的 OR 操作符 vs 字面，不扩反转义）；头注维护入口补 `_assert_refs` 路由与一条候选待办（`_row_target_text()` 反转义口径统一，触发＝出现实例）。核验：`check.sh` 全绿，语法与无 SyntaxWarning 双检通过。

- 评审配套收口（082）：`术语标准` §1 补平台标准词例外（词条名可径用 `subagent` 类宿主既定叫法，§4 维护节同步指 §1 例外）；`规格写作标准` §5.11「天然不在面内」限定为白名单形态（裸文件名形态归档件须另排）；`施工机制:150`「当前工作分支」补定义（`git branch --show-current` 返回值）；`产物契约标准` §3 目录级指针精确化（目录口径见 `命名标准` §1，指针不复写）。核验：`check.sh` 全绿、lint 0 issue、链接一行式 178 断链 0、归档零改动、`CHANGELOG` 只增一行。

- 分支残留清理与术语统一（081）：19 处 `main` 残留（13 件——件级依据与逐条〔出处〕摘去分支指称、旧仓移植头注去前缀、活规则改「直推当前工作分支」）＋60 处术语替换（8 件——旧称与英文对应对照：分身／fresh／子代理 → subagent／独立上下文，`fresh context` 为 `独立上下文` 的英文对应）＋`术语标准` 两条词条合并为一条 `subagent`（首格纯文本、列分隔半角竖线）；另落 `规格写作标准` §5 第 11 条（扫描类断言须排除本规格）、`术语表` 指向行与 `check_spec.py` 的 `_dir_split()` docstring 首句勘正。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175 条断链 0、归档零改动、`CHANGELOG` 只增行。

- 产品层按域分层（080）：`docs/product/` 按域分层——新建 `outline/`／`volume/`／`write/` 三子文件夹，七件域设计迁入（大纲域设计及其规格与资料步／草稿步／打磨定稿步／附录入 outline，卷纲域设计入 volume，正文域设计入 write；文件名一律不变，产品定义等五件共享件留顶层）；同步改写 31 条链接（README 索引行、两份标准件、验收场景与书目录契约的入链前插子文件夹前缀，移动件指回顶层出链加 `../` 前缀、跨层出链加深为 `../../`）与命名标准三处同步（§1 目录树展开、产品文档表行位置列、中文名口径句）。核验：`bash scripts/check.sh` 全绿、lint 0 issue、链接一行式 175 条断链 0、两件纯移动件 md5 不变、归档零改动。

- 尾账清理与守卫收口（078）：`check_spec.py` 汇总行改四项计数（改动行／可解析／判一适用／判二缺）、新增「C＝∅ 空转明示」（计入 findings）、`MD_LINK` 认引号包裹 title、`_dir_split()` 布尔化（行为等价）、头注补 `shell=True` 宿主语义与不可审计枚举（`python3`／`py`）；`check.sh` `[0]` 段补手动路径与按头注实况拼的收尾汇总（顶格；段序／退出码不变）；`规格写作标准` 勘正「现文」误称、`测试与验收标准` G17／[0] 行缺口同步、`施工机制` 报告纪律上移为三处通用条。核验：`bash scripts/check.sh` 全绿，10 条断言全过，全语料静态差异面恰六类：17 件汇总行改写、060 计数与基线漂移行、062 空转行、末行三态 281→282、077 自噬预警计数随动。

- 评审残留微调（079，轻路径无规格）：`施工机制` §七 归档件纪律的对照先例改并列引用（071 §九 第 3 条＝同型处置、`064` §七 第 5 条＝归档件历史记录不改——原「所引」传递链第一环失真，实测 071 该条不引 064，第四轮评审 P2-1）；`测试与验收标准` §4 新增 G18（`check.sh` 收尾汇总的段计数对头注形态破坏无容错——候选扩展位，本批不实现，P2-3）。评审 P2-2 经复核不成立（汇总行已带「无补集式断言」跳过计数）。核验：`check.sh` 全绿，lint 0 issue，链接 175／断链 0。
- 对账机制化（077）：`check_spec.py` 新增检查 E 三方对账（改动面 ↔ 禁改面 ↔ 断言排除集）——改动面逐 token 取实存件、禁改面取 `BAN_SECTION` 全节文本、豁免按子句级双条件（豁免词 ＋ 落点）、断言排除集只认 `':!<path>'` 形态；候选行计入末尾三态、汇总行只进逐件明示串；`FENCE_BLOCK` 与 `extract()` 同认 `~~~` 围栏，头注「四类」表述与维护入口同步。核验：`bash scripts/check.sh` 全绿，7 条断言通过，全语料静态差异面只新增对账行。
- 收口小批与评审报告纪律（076）：`check_spec.py` 围栏语言字符类与 `FENCE_BLOCK` 同口径、抽 `_read_target()` 统一三处目标件读取（容错解码 ＋ 换行符计数口径）、`_git_readonly()` 先移出取值型选项再令 `--` 之后一律放行；`施工机制` 立评审报告纪律（计数类发现须附实测）、交底改必带七项、scope 补「开发守卫＝`scripts`」、归档件例外改两处并列、git 写操作复述改形态判据、反模式补第 12 条；`规格写作标准` 补禁改面对账／行数口径／改写优先三条；`测试与验收标准` 段表半句、[0] 行与 G16 状态同步并新增候选 G17。核验：`bash scripts/check.sh` 全绿，7 条断言通过，静态输出逐字节不变。
- 守卫安全与口径勘正（075）：`check_spec.py` 执行面收窄（可执行族仅存 `e?grep`／`fgrep`／`wc`／`head`／`tail`／`ls`／`cat` 与 git 子命令＋参数白名单；`find`／`sed` 移出执行面、保留提取面）＋`$`／反引号引号内外一律拦＋子进程 env 置 `GIT_OPTIONAL_LOCKS=0` 并剔除注入面 → 动态阶段「注入」与「参数级写」两条通路关闭；另修活「共 N 行」判据、cache 收敛三元组、行数口径对齐 `wc -l`、目标件读取容错、stderr 同 reconfigure、认 `--fixed-strings`；`check.sh` 补非目录分支与 Python 主版本校验；三份标准件同步。核验：静态影响面＝0（语料逐字节不变，md5 `4963394d…`／290 行），`check.sh` 全绿。
- 宪法评审回应与指针勘正（074）：回应 `AGENTS.md` 评审 9 条（3 高／3 中／3 低），逐条对现文实核后采纳 5／不采纳 4——采纳者＝§六.3 例外边界（高 1）、红线 1 总括式改写（低 7）、红线 4 补「判因确证」出口（中 6）、§七 ③ 补段序指针（中 4）、§四 末句改「增删改本节条目即停」（低 9）；不采纳者＝术语双真源、npx 策略缺失、门禁机械自检、头注加版本与日期（均已在册或另经裁定）——另修三处 npx 例外指针勘正（`§一.3` → `§一.1.3`，落 `AGENTS.md`／`运行环境标准`／`测试与验收标准`）。核验：`check.sh` 全绿，6 条断言通过。
- 计数重算死块清理（073）：删去 `check_counts()` 内被整体覆盖的重复段（23 行，两段逐行相同、前段结果被后段重初始化后丢弃），只删不改不搬，行为等价（全语料静态输出逐字节不变）；其余函数、判据、输出与退出契约零改动。核验：`check.sh` 全绿；7 条断言通过（含语料比对 diff 空、md5 与行数同基线）。

- 对账覆盖缺口修补（072）：`check_spec.py` 检查 A 的零命中核对取数分两支路——子句数字个数 K＝断言数 M 时按命令序号（0 基）配对取值，K≠M 时分配型不判、全同取值、多值产出「零命中核对未判」候选行（并补入末尾三态收列表）；检查 B 的 `CLAIM` 量词扩「处」且口径收紧（整件口径限死为加粗标记计数：紧邻段含「加粗」、数字前紧邻文本不以「加」／「增」收尾，实测值取 `content.count('**')`）；`规格写作标准` §5 补第 9 条（一条断言只配一条命令与一个期望值），`测试与验收标准` §4 就地收窄 G2／G15。核验：`check.sh` 全绿；8 条断言通过（断言 7 的 hunk 上界按实测改述，定因见规格 §九）。

- AGENTS 写法借鉴（071）：`AGENTS.md` §一 红线六条与 §五 原则 2／3 各补「（自测：…）」共 8 处（同向参照 `文档写作标准` §一.8.3 的红旗自检，属自愿补强）、新增 §七「本文件在起作用时（治理文档自检信号）」（口径注 ＋ 四条现象句带真源指针，与 §一.9.2 产品层信号判据分工）＋头注补该节指针、§二 前导语补裁量分级与送审纪律指针；机械修复 `070` 归档件一条相对链接（`../` → `../../`，归档位移所致；**经作者追认，属「归档件只读」的明文例外，登记留待下批触碰 `施工机制` 时补**），并修正 070 条内 `文档写作标准`:108 的加粗计数为 91 处。核验：`check.sh` 全绿，6 条断言通过。

- 宪法一致性与修改流程补强（070）：`AGENTS.md` 头注改述＋补 §六 指针、项目一句状态表述统一、红线 1 补 git 写操作列举、红线 6 括注改指「该节判据句」、§二 路由表补文件清单指针与术语行、§四 CI／联网条改指针式例外、注册表类守卫条改四件式（保留准入句）与门禁自检不做项（两条目去加粗）、§四 末句补「解冻一律由作者裁决并记入 `CHANGELOG`」、新增 §六「本文件的修改与权威顺序」（裁决写实、例外汇到类级）；三份标准件：`运行环境标准` §一.3 立 `npx` 例外（出处补 070）＋§4 承诺句补例外指针＋「12 条」去数字化、`测试与验收标准` §1.2 补可判据＋§1.3 补例外指针、`术语标准` §3 新增四条词条；`文档写作标准`:108 的加粗计数改「91 处」并注明看性质不看数值。核验：`check.sh` 全绿，9 条断言通过。

- 分流优先与审查闭合勘正（069）：`施工机制.md` 分流表「轻」行优先级改「重优先」（同时命中「重」行触发条件时以「重」为准、否则具体优于一般），`:35` 澄清半句改按「轻」行指代、`:43` 判据来源改纯指针（不再引规则原文）；「标准」行流程列补「（触发产物审查条件时）产物审查」节点；`:63` 末支改「其余批次（不含轻路径）」＋`:92` 触发面同步轻路径例外（见 §三 第 3 步）；`:76` 静态自检段补现状指针；分身不复用条并入单维核对、止损对称条款（去粗体）与提交自检首项补轻路径例外；`规格写作标准` §1 判定表免规格行同判据改述（指代写实）。核验：`check.sh` 全绿，9 条断言通过。

- 施工机制口径勘正（068）：`施工机制.md` 十处口径统一——轻路径触发条件补全（与 `规格写作标准` §1 判定表对齐）、核验第 3 步与 §五 分工对齐、单维核对补执行主体、核销档位统一为五档、规格模板「本批核销」节围栏内清元说明（六项移为围栏外说明段）、作者出手点／外部窗口限定／归档纪律分层／[4] 段上下文／untracked 核对命令五处到位。核验：`check.sh` 全绿，8 条断言通过。

- 断言对账机械化（067）：[4] 段检查 A 的核对面由 `[A]` 行扩到 `[A]`／`[B]` 行（`[B]` 行目标文本取改动格全文），新增「零命中待复核」（期望 ≥N 而当前命中 0 时呈报，计入末尾三态判据），「跳过」行收窄为「目标件不出现在任何改动行」。规则侧：`施工机制` §三 修复后自核验补第四条（断言对账），`规格写作标准` §5 补第 8 条（计数式断言须限定扫描面，含 066 断言 4 多计 1 的反例），`测试与验收标准` §1.1 [4] 行补 `[A]`／`[B]` 面与零命中待复核。核验：`check.sh` 全绿，8 条断言通过。

- 核销入模板与补核销（066）：把 064 批新立的开放项核销机制写进 `施工机制` §八 规格模板（新增「本批核销」节——触发／核销面／写法／表骨架／汇总；原「开放项」「审查记录」顺延为八／九），§三 机制条补「相关＝有交集（同一文件／同一主题／同一产物）」判据与落点指针，§七 登记句与 `AGENTS.md` 逃生口两处枚举同步，`规格写作标准` §10.3 落点句同步；补做 064→065 那一环核销（18 条逐条标状态：仍待 16／待作者 2）。另修工具对空引号对的失效（`check_spec.py` 两处取值口径统一——此前崩溃置红，且 A 侧真预警被吞）。核验：`check.sh` 全绿，10 条断言通过。

- 规格静态自检器搬回（065）：旧仓 `check_spec_assertions.py` 移植重建为 `scripts/check_spec.py`（四类静态检查——断言自噬／计数重算／`[A]` 逐字／写作检查，判据按本仓形态重做：载体两形态、表头别名、对象口径、空转明示），接入 `check.sh` [4] 段并落 Python 三级链探测；静态发现恒 0（非阻断）。同步 `测试与验收标准`／`规格写作标准`／`运行环境标准`／`命名标准` 四件锚点行与 §4 缺口（G3／G5／G6／G7 销，G4／G8／G10／G11 留痕）。核验：`check.sh` 全绿，14 条断言通过（含双维产物审查与窄域复审）。
- 规则债清理批（064）：清四条机制债（命令行工具语义／跨层计数／加粗密度口径／待收口）＋新立开放项核销机制——`运行环境标准` §一.2 补第 6 条（`grep` 可为 ugrep 封装，守卫禁依赖转义差异）；`术语标准` 去两处产品层词条计数；`文档写作标准` §一.3.8 补计数口径／适用范围／目录要求并同步两处检查器口径（`:117` 机检锚点行与 `测试与验收标准` [3] 段）；`触发与交互标准` 撤「待收口」条；`施工机制` §三 立开放项核销机制、§七 补归档件纪律；首次核销 059–063 五份归档规格 35 条开放项。核验：`check.sh` 全绿，8 条断言通过（含规格归档）。
- 产品层内容第二批（063）：大纲域设计迁入 `docs/product/`——主件重写为十一节（新增定义性约束、跑通信号、域交互细则保留节名、灵感库接口改「可选外部资产」四条、常见问题先收 0 条），新建规格与资料步／草稿步／打磨定稿步／附录四件（源件逐字，去走查编号）；`书目录契约` :49 真源指针改指附录件、§2 状态值补「定格」与「公开样本」措辞、§4 真源列去「同上」；`术语表` 补「灵感库」词条；`README` 产品层清单补四件。核验：`check.sh` 全绿，10 条验收断言全过。
- 产品层内容第一批（062）：`架构设计` §四 由「待设计」写实——补决定性判据（共享状态是分水岭）、单／多 skill 三域化对照、旧仓实测数据（标时态「三域现态未复测」）与 T1–T3 拆分触发器；§三 逐字补入「域是平行的门，不是管道」与 Pipes and Filters 模式名；`产品定义` 填实 §四 目标用户（先自用、结构留门）与 §五 成功判据（三条，待作者校准），新增 §六 范围与不做／§七 路线，开放项顺延为 §八。核验：`check.sh` 全绿，12 条验收断言全过。
- 复盘驱动的治理微调：流程分级入 `施工机制` §二／§五——产物审查按改动语义定深度（改规则／结构／契约＝双维全审；产品层纯文档内容批＝单维核对，只查引用／数字／冲突），并新增「改叙述还是改规则」的定级判据（预立法，随首批产品层内容批校准）。
- 底座与业务分层：底座（`AGENTS.md`／`docs/specs/施工机制.md`／`docs/standards/`）去业务化——产品设计整块迁入新建 `docs/product/` 八份，术语／产物契约／测试与验收三份标准抽芯为通用方法，其余标准逐行通用化；AGENTS 不再路由产品文档。核验：`check.sh` 全绿，14 条验收断言全过。
- 基座审计整改（060）：`AGENTS.md`＋`docs/standards/` 11 份＋`施工机制.md`＋`docs/product/` 两份按 F1–F24 整改——规格自审收敛为三条并补回立法质量两条、T1 两义分流、机检锚点节号统一、三份标准补节目录、旧仓业务词与失准指向清零。核验：`check.sh` 全绿，22 条验收断言全过。
- 第三方技能仓库（`mattpocock/skills`）研究首轮落地（061）：写作方法论三子节（指针／两种负荷／下沉与修剪）、禁令须配正向目标、产品层文档范式、完成判据与 premature completion、术语优先取通用词、停靠点三级含廉价检查点、机制键实测回写四要素、不做清单补逃生口栏、规格可选节「被否决的替代方案」、守卫跳过不静默、版本一致性改可跑校验措辞、两份标准补节目录；体积判据优先级由行数改为分支（判据优先级调整，500／300 数值不变）。核验：`check.sh` 全绿，12 条验收断言全过。

### Fixed
