# Aether 项目宪法（AGENTS.md）

> 本文件是**入口与路由**：红线、路由、常用命令、不做清单、分工原则、权威顺序、模型执行通用准则。**具体标准与细则不在这里**——命名在标准里、协作在 `施工机制` 里、业务在 `docs/product/`。
>
> **开发 ≠ 运行**：本仓库（`AGENTS.md`/`docs/`/`scripts/`）是开发侧，不随产品分发；`skills/` 下是运行侧（本项目＝唯一宿主 Claude Code）。
> **机架与插件**：开发侧（本文件、标准件、施工机制、`scripts/`）为**通用机架**——换一个 skill 项目可整体复用；项目专有事实（产品词、域清单、领域规则、用户数据形态）只住 `docs/product/`、`skills/`、本文件的**项目一句**与机架内标注**「（本项目＝…）」**处——复用机架时改项目一句、按该标记检索替换。

**项目一句**：Aether——面向长篇网文创作的 AI 写作 skill 套件（运行宿主唯一：Claude Code）；业务设计见 `docs/product/`；当前以开发侧基座为主。〔2026-09-19 由 Abzu 更名〕

## 目录

- 一、红线（违反即停）
- 二、路由
- 三、常用命令
- 四、不做清单（以本节为准）
- 五、三条分工原则
- 六、修改与权威顺序
- 七、自检信号
- 八、模型执行通用准则

## 一、红线（违反即停）

| # | 规则 | 自测 |
| --- | --- | --- |
| 1 | **git 默认只读**——凡改变仓库状态的 git 命令，每次单独取得作者明确确认（`fetch` 等只动 `.git/` 者在内；对其他操作的授权不外延到 git） | 这条 git 命令会改仓库吗？会＝确认拿到了吗？ |
| 2 | **临时产物不入库**——临时脚本与输出放 `.tmp/<任务>/`，用完即删（例外：工具内部临时件，如 `patch_file.py` 落目标件同目录的 `.patch-tmp`——同目录是原子替换所必需，信号中断的残留见其已知限制①） | 这是临时件吗？＝在 `.tmp/` 且收工即删吗？（工具内部临时件按规则列括注豁免） |
| 3 | **外部参考只读**——不在外部项目/参考目录中安装依赖、跑构建、跑联网命令 | 会让外部目录变化吗？ |
| 4 | **失败先判因**——禁止改断言/改数字/删检查来变绿；合法路径=呈报→裁定→修订规格→再改（见 [`施工机制`](docs/specs/施工机制.md) §七） | 我改的是原因，还是断言？ |
| 5 | **密钥不入库**——token/key/密码/`.env` 用环境变量 | 里面有密钥字面吗？ |
| 6 | **禁带病开工**——动工前 `bash scripts/check.sh` 全绿，且过目各段跳过与候选行（全绿≠候选面为零）；红起点定义见 [`施工机制`](docs/specs/施工机制.md) §六 | 我跑了 check.sh 且读了候选，还是只记得它绿？ |

## 二、路由

**动手改任何内容前**：读 [`施工机制`](docs/specs/施工机制.md) §二——AI 按改动量定**模式**（简单／标准／全量）并呈报，**你拍板**。

| 任务 | 去哪份 |
| --- | --- |
| 写 skill／域资产（形态、frontmatter、触发、交互、运行时、内容矩阵） | [`skill资产标准`](docs/standards/skill资产标准.md) |
| 命名／用词／术语／文档写法（含禁用清单） | [`文字与命名标准`](docs/standards/文字与命名标准.md) |
| 产物字段契约 | [`产物契约标准`](docs/standards/产物契约标准.md) |
| 运行环境／安全（宿主、依赖、凭据、外部边界、脚本契约） | [`运行环境标准`](docs/standards/运行环境标准.md) |
| 协作施工（工作方式／核验／审查／提交） | [`施工机制`](docs/specs/施工机制.md) |
| 写规格 | `施工机制` §九 模板（规格模板＋自审五条＋断言写法七条；自审五条按 §四 留痕表形态落——检查 M 的核对对象） |
| 加／改守卫 | `施工机制` §六 ＋ `check.sh` 头注（段序以头注为准）＋各标准件 §三 表；规格自检器＝`scripts/check_spec.py`；触检查脚本时规格须含「需同步更新的文件核对」表（检查 E 核对③） |
| 事故记录／总结经验 | [《施工问题记录》](docs/specs/施工问题记录.md)（踩坑表＋填写规则＋总结方法） |
| 在制与历史规格 | [`docs/specs/`](docs/specs/) 与 [`docs/specs/archive/`](docs/specs/archive/) |
| 产品层设计 | `docs/product/` 对应域设计 |
| 沿革（为什么这么改） | [`CHANGELOG.md`](CHANGELOG.md) 与 git 历史 |
| 术语叫法 | [`术语表`](docs/product/术语表.md)；治理类词条见 [`文字与命名标准`](docs/standards/文字与命名标准.md) §9 |

（开工说明／自查按上表点名，不再逐批手写。）

## 三、常用命令

- 一键检查（提交前必跑）：`bash scripts/check.sh`
- 改动文件核对（提交前）：`python scripts/check_spec.py --reconcile <规格>`（清单外核对）；`--verify`（过程产物核验，归档时用）——两模式不进 `check.sh`，人工按需
- `scripts/patch_file.py` 写文件属工作流内动作，不触发红线 1（其只管 git 命令），仍守红线 4／6
- 提交前自动检查启用（一次性）：`git config core.hooksPath scripts/hooks`（改 `.git/config`，须你确认——红线 1）
- Markdown 体检：`npx markdownlint-cli2 "**/*.md"`
- Windows（Git Bash）速查——跑命令前先过这七行，**细节与完整清单见[运行环境标准](docs/standards/运行环境标准.md) §2**：
  1. **中文＝字节陷阱**：`wc -c` 算字节（中文 UTF-8＝3 字节/字）；要字符数用 `python -c "print(len(open('f',encoding='utf-8').read()))"`。
  2. **`grep -c` 算行不算次**：要出现次数用 `grep -o 'X' f | wc -l`；两值不等。
  3. **管道吞退出码**：`cmd | tail` 后 `$?` 是 `tail` 的——必须接管道时取 `PIPESTATUS[0]`。
  4. **进程替换 `<(...)` 不可靠**：Git Bash 下 fd 路径可能被拼坏——用临时文件或 `tee` 替代。
  5. **Python 调用**：命令行手写调用用 `python`；工具内探测以[运行环境标准](docs/standards/运行环境标准.md) §一.4 三级链为准（`python3 → python → py` 依次探测）；中文输出前 `export PYTHONIOENCODING=utf-8`；路径一律正斜杠。
  6. **内联脚本重定向**：`python -c "…"` 里的 `>` 在未加引号或引号提前闭合时会被 shell 当重定向、建出垃圾文件——引号包全即安全，多用 `<<'EOF'` 写法。
  7. **写文件用专用工具**：文件修改一律专用写文件工具（`Edit`／`Write`／`scripts/patch_file.py`）——**bash heredoc 包多行 Python 的补丁脚本禁用**（119 实测单批 6 次锚定失败循环；JSON→shell→Python 多层会把 `\n`／`\u0000` 塌成真实换行／NUL 字节，历批三起）；只读计数可用单行 `python -c`；写入后语法预检。

## 四、不做清单（以本节为准）

> 格式：不做 ＋ 解冻触发关键词（完整理由见 git 历史原版本）。增删改本节须作者确认；解冻由作者裁决记入 CHANGELOG。

**继承**（各条括注＝解冻触发关键词）：

- RAG/向量检索（本项目＝单书体量超人工与 grep 可定位、且实测成瓶颈）
- 数据库后端（数据正本须并发多写或多端同编）
- 常驻服务/Dashboard（须实时刷新或多人在线查看）
- 多宿主适配（作者裁定增第二宿主）
- CI 跑 LLM/联网（须无人值守且仍零外部依赖）
- 拦截式宿主 hook（不拦即不可逆损失且实测非拦不可）

**追加**（同格式）：

- 平行清单（单批记录规格载不下、且查证成本成实际瓶颈）
- 无事故出身注册表守卫（同类事故复发且可零依赖机械化）
- shared-assets 多副本（出现无法用引用表达的共享资产）
- CI workflow（多人协作、或须远端环境才能复现的检查）
- 多人协作治理件（收到外部 PR）
- 提交前自动检查启用机械自检（未设 hooksPath 而漏拦截的实际事故）

（完整理由与解冻条件见 git 历史原版本〔`578815a` 及此前的本节〕——速查索引即现行法，详注供追溯。）

引入新机制前过**内生必要性自问**：能否不新增 → 能否并入既有文件 → 必须新增时的验收标准与索引同步。

## 五、三条分工原则

1. **脚本做确定性、AI 做语义、作者做品味**
2. **候选永不拦截**——机器检出的候选类只呈报，不阻断、不影响退出码
3. **文件账本律**——工作流每一步须落文件；仅「规格在会话内呈报」这一动作不单独计为一步，落文件后才计

## 六、修改与权威顺序

1. 修改本文件须作者确认。
2. **权威顺序**：宪法级约束 ＞ `docs/standards/` ＞ `docs/specs/` ＞ 其他。
3. **具体指定优先于层级**——明文指定「以某件为准」（如协作以 `施工机制` 为准、术语以术语表为准）时，指定优先；指定不得抵触红线/不做清单/分工原则；指定冲突按 `施工机制` §七升级。

## 七、自检信号

本节为治理文档信号；产品层信号（跑通信号）见 `文字与命名标准` §16——两者判定标准不同，不得互引为据。

| 信号 | 判定标准 | 以何为准 |
| --- | --- | --- |
| 数字可复算 | 重跑与规格一致 | `施工机制` §九（规格自审五条／断言写法） |
| 拦下问题的是机器与审查 | 提交无清单外文件 | `施工机制` §五 |
| 工具报得出与应报出一致 | [4] 段 A–E、H–M 无未核项（＝十一类均实际执行、无「全部跳过」行、跳过行逐条有因） | `check.sh` 头注 |
| 每批可追溯 | 规格+CHANGELOG+git 三者还原 | `施工机制` §五 |

## 八、模型执行通用准则

> 本节是保底做法：标准与《施工机制》有规定的从其规定；没规定的按本节执行。

1. **动手前**：按 §二 路由读对应文件；**并检索管辖所改资产的条文**——所改资产归谁管，就先过谁的规定（改 skill 资产 → 过 `skill资产标准` §7 交互模态、《文字与命名标准》§6 禁用清单与 §13 行文规则；改治理文档 → 过 §13 行文规则；改脚本 → 过 `运行环境标准` §一 与 `check.sh` 头注）。**实证（一小时内两族各两次）**：写条文未查禁用清单 → 用词扫描连逮两次；改交互形态未查 §7 → 写下与「候选超 4 收高频 4 项」「同一屏不混模态」相抵的规格，被标准打回。读完仍不确定怎么做，停下向作者呈报，不凭猜测动工。
2. **表达**：一切产出——**与作者的对话回复**、文档、规格、注释、提交信息、脚本输出——统一用简洁、直白、通俗、唯一语义的中文；杜绝抽象晦涩的专业黑话与陌生术语，同一概念只用一个词，短句直陈、结论先行、删字不删条件，不确定就问、不造词。用词与行文细则见[《文字与命名标准》](docs/standards/文字与命名标准.md) §5–§6、§13。
3. **改动范围**：只改任务点名的文件；发现必须同时改其他文件时，先呈报、征得同意再改。
4. **检查失败**：先找原因再改；禁止改断言、改数字、删检查来变绿（红线 4）；找不到原因即停下呈报。
5. **规则冲突**：按 §六 权威顺序取高者；同层级两处规则冲突时停下呈报，不自行选边。
6. **默认不做**：不新增机制、不另建记录清单、不引外部依赖、不跑联网命令（先过 §四 不做清单与内生必要性自问）。
7. **收尾必做**：`bash scripts/check.sh` 全绿；按 `施工机制` §五 登记（`CHANGELOG` 只落用户可感知变更，内部治理改动住 git commit message）。
